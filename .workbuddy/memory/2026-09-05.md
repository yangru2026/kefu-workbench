
## 2026-09-05 质检保存报 id not-null（5a71fe4）
- 报错：insert qc_records 报 null value in column "id"——云端表 id 无自增默认值（疑 fix_qc_records_id_type.sql 重跑重建所致）；qc-v2 前端本身不带 id，纯云端问题
- 修复：fix_qc_records_id_type.sql 建表语句改 GENERATED BY DEFAULT AS IDENTITY；旧版 qc.html 新增不再显式传 id:null + maybeSingle
- 待用户执行：qc_fix_id_identity.sql（幂等，补 identity + setval + 干跑验证）

## 2026-09-05 质检 id 报错二次排查
- identity 已加（BY DEFAULT 确认）但用户仍报 null id → 判断为浏览器缓存旧 qc-v2（fa677d6 时期曾有 id:null 版本）显式传 null 绕过 identity
- 对策：outputs/sql-qc-id-null-guard.sql 兜底触发器（NEW.id IS NULL → nextval 序列）+ NOTIFY pgrst reload + 显式null干跑验证；待用户执行

- 14:51 用户跑 V1 兜底后干跑A(显式null)仍报23502，trigger 未拦截；发 V2（自诊断+独立序列兜底），等 Messages 诊断输出

- V2 脚本有笔误（END \$f; 少一个\$导致动态建函数 42601），V3 重写：DROP IDENTITY → 显式序列默认 → 同一序列 trigger 兜底，无嵌套无歧义；等用户执行结果

- 15:06 前端仍报 id null（V3 是否跑成未确认）；另修粘贴 bug：paste 监听从 paste-zone 提升到 document 捕获级（弹窗 active 时任意位置可贴，zone 内贴入对应区），iframe v 升 20260905f（9a7ff76）

## 15:2x qc-fix-copy.html 一键复制页部署（commit ac4a543）
- 用户聊天复制 SQL 被污染（42601 more target columns）→ 改交付方式：`outputs/qc-fix-copy.html` push 到 Pages。
- URL: https://yangru2026.github.io/kefu-workbench/outputs/qc-fix-copy.html （已 curl 验证 200）
- 内嵌 V3 SQL（1376 字符），clipboard API + execCommand fallback，页面含操作四步指引。
- 待用户执行 V3 成功后闭环 qc_records 23502 问题。

## 15:22 V3 失败根因确认 → V4 部署（commit 8119379）
- V3 截图：check A（显式 NULL id）通过 = 触发器/序列已生效；check B 报 23502。
- **根因：脚本内 check A 的 `ROLLBACK;` 把整批事务（含全部 DDL）一起回滚**，check B 在无修复状态执行 → 假失败。Supabase SQL Editor 整批单事务再次证实。
- V4 = 纯 DDL 无验证回滚 + 末尾只读确认查询（column_default + pg_trigger）。
- 页面: https://yangru2026.github.io/kefu-workbench/outputs/qc-fix-copy-v4.html （已验证 200）
- 教训沉淀：给用户的 SQL 一律不要内嵌 BEGIN/ROLLBACK 验证块；验证用只读 SELECT。

## 15:29 V4 执行成功 ✅
- 截图确认：无红字，Results 显示 trigger_name = trg_qc_records_fix_id。qc_records 修复落库。
- 待用户强刷质检页验证保存 + 粘贴贴图。
