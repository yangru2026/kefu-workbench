# 2026-08-08

## lightbox 大图弹窗居中修复

**用户反馈**：lightbox 大图打开后位置偏左下，不是居中，有遮挡。

**修复内容**：`index.html` 中 lightbox 结构简化
- 根因：高清图 `.lightbox-hd` 使用 `position:absolute; top:0; left:0;`，叠加在缩略图上，但未设置宽高撑满容器，导致图片显示在容器左上角，视觉上偏左下。
- 解决：移除已废弃的 `#lightbox-thumb` 模糊占位图，将 `#lightbox-img` 改为普通 flex item，依赖 `.lightbox-img-stack` 的 `align-items:center; justify-content:center;` 自然居中；loading spinner 使用 `position:absolute; inset:0;` 覆盖在中央。
- 提交：`859bae7`，已 push 到 GitHub Pages。

## 飞书多表同步 - 排班表/客服排名/售前月度数据

**用户需求**：排班表、客服排名、售前数据三块关联飞书表格同步；售前数据需要 A/B/C 三组按月汇总、存档、补录、更改。

**实现内容**：
1. **飞书表格设计**（`feishu-tables-design.md`）：排班表（月份+姓名+组别+1~31日）、客服排名（月份+姓名+组别+4指标）、售前月度（月份+组别+10指标）
2. **SQL 迁移**（`feishu_multi_sync_migration.sql`）：新建 schedule_data + presale_monthly 表，修改 ranking_data 表（加 staff_name + feishu_record_id），RLS + Realtime
3. **Edge Function 改造**（`supabase/functions/feishu-sync/index.ts`）：支持 action 参数区分 4 种同步类型（patterns/schedule/ranking/presale），每种有独立字段映射和 Supabase 目标表
4. **前端改造**（`index.html`）：
   - 排班表：从 Supabase 读取，加飞书同步按钮，单元格编辑自动保存
   - 客服排名：从 Supabase 读取，加月份选择器，编辑后保存到 Supabase
   - 售前数据：新增每日/月度 tab，月度 tab 展示 A/B/C 三组数据，支持编辑/补录/保存
5. **部署指南**（`feishu-multi-sync-deploy-guide.md`）：完整 6 步部署流程

**待用户操作**：
- 在飞书创建 3 张多维表格
- 执行 SQL 迁移
- 部署 Edge Function + 配置环境变量
- 配置前端 authToken

提交：`2375e2c`，已 push。

## 花色素材页加载性能优化（A+B+C 全做）

**用户反馈**：花色素材页加载太慢，客服宁愿用飞书表格也不愿意等工作台。

**问题诊断**：
- 357 张花色原图共 66.3MB，平均每张 190KB，最大 1.1MB
- 首屏 5×5=25 张可见图 = ~5MB 数据量，正常网络下要 5-10 秒
- 附带代码 bug：`app.js` 第 14 行 `onSupabaseReady` 在 patterns 页重复调 `loadPatternsFromDB + renderPatternFilters + renderPatterns`，与 `loadPatternsFromDB` 内部的 render 调用叠加 = **每次进入花色页重复渲染 2 遍**

### 方案 A：代码层修复（已生效）
- 修复 `app.js` 第 14 行重复调用 bug（`onSupabaseReady` 进入 patterns 页时改为只兜底检查数据是否加载，不重复调 render）
- `<head>` 加 preconnect/dns-prefetch 到 yangru2026.github.io 和 supabase.co
- 预期提速：20-30%

### 方案 B：缩略图（核心优化，预期提速 6 倍）
- 写 `generate_thumbnails.py`：扫描 `images/patterns/*.jpg`，用 PIL 生成 480px WebP 缩略图到 `images/patterns/thumb/`
- **结果**：357/357 全部成功，66.3MB → 11.3MB（**节省 83%**），平均每张 33KB
- 数据库加 `thumb_eye_url` / `thumb_lens_url` 字段
- `renderPatterns` 改写：默认用缩略图；hover 切换的 lens img 用 `data-lens-src` 延迟加载（hover 时 JS 替换 src）；前 6 张加 `fetchpriority="high"` + `decoding="async"`
- 缩略图加载中显示 shimmer 渐变占位
- `add_thumb_columns.sql`：一键 ADD COLUMN + 自动批量回填 thumb URL（用 REGEXP_REPLACE）

### 方案 C：分页/加载更多
- `patternPage` 状态 + `PATTERN_PAGE_SIZE = 20`
- `renderPatterns` 切片渲染，底部显示"加载更多"按钮 + 剩余数量
- 切换筛选/搜索/品牌/抛型/系列/色系/下架时自动重置 `patternPage = 1`

### 改动文件
- `index.html`：缩略图渲染 + 分页 + 加载更多 CSS + head preconnect + buildPatternTree 加 thumb 字段
- `app.js`：第 14 行修复
- `add_thumb_columns.sql`：新文件，DB 迁移+回填
- `generate_thumbnails.py`：新文件，缩略图生成脚本
- `images/patterns/thumb/*.webp`：357 个新缩略图

### 用户需要做的
1. 在 Supabase SQL Editor 执行 `add_thumb_columns.sql`（添加 thumb_eye_url / thumb_lens_url 字段并批量回填）
2. 等待 GitHub Pages 部署完成（commit 3d87f91 已 push）

### 预期效果
- 首屏数据量：5MB → 830KB（降低 83%）
- 首屏渲染：8秒 → 1-1.5秒（提速 6 倍）
- 加载更多：20 个/页，避免一次性渲染所有 DOM

### 注意事项
- 缩略图 WebP 兼容性：所有现代浏览器（Chrome/Firefox/Edge/Safari 14+）都支持，客服电脑应该都没问题
- 老数据兼容：thumb_eye_url / thumb_lens_url 为空时自动回退到原图（已实现）
- 后续：飞书同步 Edge Function 已支持 thumb_eye_url / thumb_lens_url 字段（需要更新字段映射）

## 花色素材图片 404 问题诊断与修复

**用户反馈**：执行完 `add_thumb_columns.sql` 后，原图和缩略图 URL 都 404。

**排查过程**：
- curl 测试 GitHub Pages 上 `images/patterns/misho_daily_一夜鲍富_eye.jpg`（URL编码后）→ 200 OK（文件存在且可访问）
- curl 测试缩略图 `images/patterns/thumb/misho_daily_一夜鲍富_eye.webp` → 200 OK
- 用户访问 `misho_daily_小坏坏pro_eye.jpg` → 404（本地不存在此文件名）
- 诊断：数据库里部分记录的图片路径与本地实际文件名不匹配（可能是飞书同步覆盖了路径）

**创建的诊断工具**：
- `diagnose.html`：自包含诊断页面，嵌入 357 个本地文件名列表，用 supabase-js 查询数据库，自动对比路径前缀（中文/英文）+ 文件名匹配 + 模糊匹配建议 + 一键生成修复 SQL
- `fix_pattern_paths.sql`：手动修复 SQL（中文路径前缀 → 英文 + 重新生成缩略图 URL）
- 已 push 到 GitHub Pages（commit c39daf6），可在线访问 `https://yangru2026.github.io/kefu-workbench/diagnose.html`

**待用户操作**：
1. 打开 diagnose.html（本地或在线），看诊断结果
2. 点「复制修复 SQL」，粘贴到 Supabase SQL Editor 执行
3. 等 GitHub Pages 部署完成后刷新工作台验证

## 花色素材交互优化（2026-08-08 中午）

**用户反馈**：
- 顶部的「下架花色」大分类按钮不想要
- 下架标记希望放在卡片上，同时生成像「抛型/系列/色系」那样的小分类
- lightbox 点开大图加载慢

**改动**：
- 去掉 `pattern-brand-tabs` 里的「📦 下架花色」大分类按钮
- 新增 `pattern-filter-status` 状态筛选行：全部 / 在售 / 下架（与抛型/系列/色系同级小分类）
- `patternShowDiscontinued` 改为 `patternStatusFilter`（'全部'|'在售'|'下架'），切换品牌/抛型/系列/色系/搜索时重置为「全部」
- lightbox 大图改为渐进加载：先显示模糊缩略图占位 → 异步加载高清原图 → 加载完成后淡入高清图
- `navLightbox` 预加载下一张高清图，左右切换更顺滑
- 卡片 lightbox 数据由单 src 改为 `{thumb, hd, type, hint}` 双 URL

**已 push**：commit `19eaf29`

**待沟通**：
- 排班表和客服排名也关联飞书表格同步，需要确认表格结构

## 花色素材四项改进（2026-08-08 下午）

用户反馈4项需求，全部完成：

### 1. Lightbox 大图加载优化（Task #130 ✅）
- 限制容器最大宽度 900px 避免小图过度放大模糊
- blur 从 10px 降到 8px，brightness 从 0.85 降到 0.75 提升缩略图可见度
- 新增2秒超时提示"图片较大，正在加载..."和失败重试按钮
- 取消旧 preload 回调防止竞态条件

### 2. 卡片下架标记（Task #131 ✅）
- meta-row 中显示"已下架"标签（橙色背景）
- 花色名称加删除线 + 降低透明度
- 与已有图片角标配合，客服一眼识别下架花色

### 3. 直径分类筛选维度（Task #132 ✅）
- 新增 `diameter` 作为第5个分类维度（品牌>抛型>直径>系列>色系>状态）
- `add_diameter_category.sql`: 修改 CHECK 约束 + 从 pattern_assets 自动提取直径值
- `getFilteredItems` 增加 diameter 参数，级联筛选
- 直径筛选行 HTML/CSS/JS 与现有维度结构一致
- CRUD 完整支持：添加/编辑/删除/右键菜单

### 4. 色系分类合并整理（Task #133 ✅）
- **SQL 迁移脚本** `merge_color_categories.sql`:
  - 将60+非标准色系值（如"黑"、"棕"、"青灰"、"棕粉"、"黑灰+亮点"等）按关键词智能归一到9个标准分类
  - 映射规则：含"黑"+"灰"→黑灰色系，含"青灰"→灰色系，含"棕粉"→粉色系，含"拼色"→混血色等
  - 合并"黑色系"→"黑灰色系"减少分类数量
  - 清理 pattern_categories 表中非标准色系记录，重新排序
- **前端色系合并对话框**:
  - `deletePatCat` 对 color 类型改为弹出合并对话框（不再简单清空）
  - 两个选项：「合并到其他色系」(推荐，下拉选择目标) 和「清空色系字段」
  - `executeColorMerge()`: 将该色系下所有花色 color 字段批量更新为目标色系
- 编辑表单色系下拉回退列表增加"粉紫色系"

**已 push**: commit `a7ec0a3`
**用户需要做的**: 在 Supabase SQL Editor 执行 `merge_color_categories.sql`

## 花色素材继续优化（2026-08-08 傍晚）

用户反馈4个问题，全部修复：

### 1. Lightbox 高清图加载仍慢（Task #134 ✅）
- 新增 `generate_large_images.py`：批量生成 900px WebP 中图到 `images/patterns/large/`
- 357/357 成功，原图 66.3MB → 中图 27.9MB（节省 38.4MB），平均 80KB/张
- Lightbox 优先加载中图，中图失败自动回退原图
- 超时提示从 2秒缩短到 1.5秒，文案改为"正在加载高清图…"
- `navLightbox` 预加载下一张中图

### 2. 直径值重复（14.5 与 14.5mm）（Task #135 ✅）
- 新增 `normalize_diameter.sql`：
  - 清理 diameter 字段中的 mm 后缀、空格（`14.5mm` → `14.5`）
  - 删除并重建 pattern_categories 中 diameter 类型分类
  - 按数值排序
- 前端添加直径分类时自动去 mm 后缀

### 3. 删除分类只想合并不想清空（Task #136 ✅）
- 将色系合并对话框通用化：`cat-merge-modal`
- 删除 diameter / type / series / color 任意分类时，都支持「合并到其他分类」或「清空字段」
- 品牌（brand）因涉及顶层分组，仍要求先转移花色再删除

### 4. 花色卡片没有下架功能入口（Task #137 ✅）
- 管理按钮（编辑/下架/删除）原本隐藏在「管理模式」开关后，用户找不到
- 改为管理员直接可见（CSS `display:flex`），按钮加大到 28px 并增强阴影/背景对比
- 无需先点「管理素材」即可直接标记下架/取消下架

**已 push**: commit `4cce91f`
**用户需要做的**: 在 Supabase SQL Editor 执行 `normalize_diameter.sql`

## 花色素材问题修复（2026-08-08 晚上）

用户反馈3个问题：

### 1. 标记下架提示缺少 is_discontinued 字段
- 原因：`pattern_assets` 表还没加 `is_discontinued` 字段
- 提供 `add_discontinued_column.sql`：ADD COLUMN + 索引
- **用户需要做的**：在 Supabase SQL Editor 执行该 SQL

### 2. 直径 SQL 执行报错（CHECK 约束）
- 错误：`new row for relation "pattern_categories" violates check constraint "pattern_categories_category_type_check"`
- 原因：数据库的 CHECK 约束还没包含 'diameter'
- 修复：`normalize_diameter.sql` 开头增加 ALTER TABLE 扩展 CHECK 约束
- **已 push**: commit `be91cd5`

### 3. 切换分类图片加载仍慢
- 原因1：浏览器缓存了旧图片资源
- 原因2：hover 切换 lens 时加载的是原图，不是中图
- 修复：
  - 所有本地图片 URL 加 `?v=2` 缓存版本号，强制刷新到最新资源
  - 新增 `toLargeImageUrl()` 统一生成中图路径，兼容相对/绝对路径
  - hover 切换 lens 时优先加载 `large/` 中图，没有中图才回退原图
- **已 push**: commit `be91cd5`

**用户需要做的**（按顺序）：
1. 执行 `add_discontinued_column.sql` 添加下架字段
2. 重新执行修复后的 `normalize_diameter.sql` 归一化直径
3. 等 GitHub Pages 部署完成后刷新工作台（建议按 `Ctrl+F5` 强制刷新）

## Lightbox 高清图加载慢 — 彻底解决（2026-08-08 深夜）

**用户反馈**：lightbox 打开高清图仍然很慢，显示"正在加载高清图…"转圈。

**根因分析**：
- GitHub Pages 在国内访问慢，即使中图平均 80KB，加载也常超过 1.5 秒
- lightbox 先显示模糊缩略图 + loader，用户感知就是"卡"

**彻底解决三板斧**：

### 1. 中图进一步压缩
- `generate_large_images.py`：中图从 900px/quality 82 降到 **600px/quality 70**
- 357/357 成功，中图总大小从 **27.9MB → 11.8MB**，平均 **34KB/张**
- 600px 在 lightbox 最大 900px 容器内已足够清晰

### 2. 全站图片走 jsDelivr CDN
- 新增 `toCdnUrl()`：把 `images/patterns/...` 和 `https://yangru2026.github.io/kefu-workbench/...` 统一转成 `https://cdn.jsdelivr.net/gh/yangru2026/kefu-workbench@main/...`
- jsDelivr 在国内有节点，访问速度显著快于 GitHub Pages
- 缩略图、原图、中图全部走 CDN

### 3. lightbox 交互重构（不再转圈）
- 点击瞬间直接显示**已缓存的缩略图**（不再模糊，仅轻微降亮度提示"高清加载中"）
- 后台**静默加载**高清中图，加载完成后淡入替换
- 加载失败自动回退原图 CDN；再失败保持缩略图可见，**不显示转圈/错误**
- 去掉"正在加载高清图…"的焦虑提示，改为 1.5 秒后极淡的"正在优化清晰度…"

**已 push**: commit `b0d7582`

**用户需要做的**：
1. 等 1-2 分钟 GitHub Pages 部署完成
2. 打开工作台花色素材页
3. 点击任意花色大图，应该立即看到图片，1-3 秒内自动变清晰
4. 如果还是旧效果，按 `Ctrl+F5` 强制刷新（清除浏览器缓存）

**注意**：jsDelivr 对 GitHub 文件有缓存，新 push 的 600px 中图可能需要几分钟到几小时才能全球刷新。如果点开还是慢，等 10 分钟后再试。

## Lightbox 大图仍模糊 — 升级到 1200px 高清中图（2026-08-08 下午）

**用户反馈**：lightbox 大图仍然很模糊，600px 中图在 900px 容器内被放大导致不清晰。

**修复**：
- `generate_large_images.py`：中图从 600px/quality 70 升级到 **1200px/quality 82**
- 357/357 成功，中图总大小 **36.3MB**，平均 **104KB/张**，比原图节省 30MB
- 1200px 超过 lightbox 容器最大 900px，保证高清不模糊
- `toLargeImageUrl()` 返回的 URL 统一加 `?v=3` 版本号，强制刷新 jsDelivr CDN 旧缓存
- 缩略图/原图/中图全部加 `?v=3`

**已 push**: commit `f6cf0d6`

**用户需要做的**：
1. 等 1-2 分钟 GitHub Pages 部署完成
2. 按 `Ctrl+F5` 强制刷新工作台
3. 点开花色大图，1-3 秒内应变为高清
4. 如仍模糊，可右键 lightbox 图片 →「在新标签页打开图片」，检查 URL 是否包含 `/patterns/hd/`、`.webp?v=3`，且图片尺寸为 1200px

## Lightbox 最终修复 — 不再模糊+不再跳转（2026-08-08 下午 14:19）

**用户反馈**：①大图仍然模糊；②先显示模糊缩略图再变清晰（体验差）；③点复制会跳转到新网页。

**根因**：
1. 模糊：jsDelivr CDN 缓存了旧的 600px `large/` 目录文件，`?v=3` 参数未能完全绕过缓存
2. 先糊后清晰：lightbox 先显示 200px 缩略图占位，再加载中图替换
3. 跳转：`copyLightboxImg` 的最终 fallback 用了 `window.open(url, '_blank')`

**修复**：
1. 中图目录从 `large/` 改为 **`hd/`**，彻底避开 jsDelivr 旧缓存
2. lightbox **不再用缩略图占位**：直接显示 spinner → 加载完 1200px 中图 → 淡入显示
3. 图片加载加 **`crossOrigin='anonymous'`**，canvas 复制不再跨域报错
4. 复制功能修复：canvas → fetch blob → 下载文件，**不再用 `window.open` 跳转网页**

**已 push**: commit `85298ff`

**用户需要做的**：
1. 等 1-2 分钟 GitHub Pages 部署
2. 按 `Ctrl + F5` 强制刷新工作台
3. 点开花色大图：spinner → 1-3 秒直接显示 1200px 高清图（不再先糊后清晰）
4. 点复制图片：直接复制到剪贴板或下载文件（不再跳转网页）

## 客服排名 & 售前月度数据支持 Excel/CSV 导入（2026-08-08 下午 16:30）

**用户需求**：客服排名和售前月度数据能不能导入 Excel 表格，不想手工一个个录入。

**实现内容**：
1. 引入 SheetJS (`xlsx@0.18.5`) 解析 `.xlsx/.xls/.csv` 文件
2. 客服排名页新增「📥 导入Excel」按钮（admin-only）
3. 售前月度汇总页新增「📥 导入Excel」按钮（admin-only）
4. 自动识别中文/英文表头，支持多种月份格式（`2026-08`、`2026/08`、`2026年08月`、`2026.08`）
5. 导入后自动按唯一键 upsert 到 Supabase（排名：`staff_name,period`；售前月度：`period_month,group_name`）
6. 导入完成自动刷新当前页面，错误行给出提示

**客服排名 Excel 格式要求**：
| 月份 | 姓名 | 组别 | 接待量 | 询单人数 | 付款人数 | 转化率 | 连带销售额 | 响应时间 | 满意度 |

**售前月度 Excel 格式要求**：
| 月份 | 组别 | 接待量 | 成交单数 | 销售额 | 转化率 | 响应时间 | 满意度 | 连带销售额 | 接待人数 | 询单人数 | 付款人数 |

## 飞书多表同步部署上线

**背景**：用户习惯在飞书表格里维护数据，不想在工作台重复修改。需要飞书→工作台单向同步。

**飞书配置**：
- 飞书自建应用 App ID: `cli_aafc447873a1dbc3`
- 多维表格 app_token: `GQBkbeK4raE8k6s1zQOcGgHlnZf`
- 排班表 table_id: `tblIbNRnDbAzjPyg`（18条记录，已同步成功）
- 客服排名 table_id: `tblT3Hs7He9G1XIb`（缺接待量/询单人数/付款人数3列，同步时填0）
- 售前月度 table_id: `tblHbb3l8pNbROcy`（12个字段全部匹配）

**部署步骤**：
1. Supabase CLI（npx supabase）安装到 node workspace
2. `supabase link --project-ref ienmejlxukhrxjjxvfqf` 链接项目
3. `supabase functions deploy feishu-sync --no-verify-jwt` 部署 Edge Function
4. `supabase secrets set` 配置 7 个环境变量（FEISHU_APP_ID/APP_SECRET/BITABLE_APP_TOKEN/SCHEDULE_TABLE_ID/RANKING_TABLE_ID/PRESALE_TABLE_ID/SYNC_AUTH_TOKEN）
5. 前端 `FEISHU_SYNC_CONFIG.authToken` 填入 `sync_1786182307_8d204d969f0b99fd`
6. commit `ca45f1e` push 到 GitHub Pages

**SYNC_AUTH_TOKEN**: `sync_1786182307_8d204d969f0b99fd`

**注意**：FEISHU_PATTERN_TABLE_ID 未设置（花色素材的飞书表格在另一个 bitable，如需同步花色素材需单独配置）

## 飞书同步花色素材 - 空值不覆盖修复

**问题**：用户飞书表格没有图片字段，同步会用空字符串覆盖数据库已有的 lens_img/eye_img。
**修复**：`mapPatternRecord` 改为条件写入——只有飞书表格里有值的字段才写入 Supabase，空值字段不包含在 upsert payload 中，PostgreSQL merge-duplicates 不会动已有数据。
- name/brand（冲突键）和 feishu_record_id 始终写入
- 其余 12 个字段（type/series/color/diameter/color_diameter/material/oxygen/water/spec/lens_img/eye_img/description）飞书有值才写入
- is_discontinued 也改为有值才写入
- commit `ef1f945`，Edge Function 已重新部署

## 客服排名表改版

- **列序调整**: 花名→接待量→询单人数→付款人数→转化率→连带销售额→响应→满意度
- **新增字段**: visitors(接待量)、inquiry_count(询单人数)、payment_count(付款人数)，需执行 `add_ranking_columns.sql`
- **去掉A/B/C组切换**: 所有组同屏展示，每组有组汇总行，最底部有总计行
- **汇总规则**: 接待量/询单/付款/连带销售额 求和，响应/满意度/转化率 取平均
- **Excel导入**: 同步支持新字段，表头智能识别（接待量/visitors、询单人数/inquiry_count、付款人数/payment_count 等）
- 提交: `d6ecda7`，已 push

**已 push**: commit `4e346fd`

## 花色素材飞书同步改为 insert-only + 新款置顶

- **Edge Function 改造**: 花色素材同步从 upsert(merge-duplicates) 改为 insert-only(ignore-duplicates)，已存在的花色完全不动（图片、下架标记等全部保留）
- **移除标记下架逻辑**: 飞书表格只放新款，不能用来判断哪些花色该下架，移除 postSyncFn 中的 markStalePatternsDiscontinued
- **新款置顶排序**: renderPatterns() 中按 created_at 降序排列，最新添加的排最前面
- **🆕 新款标签**: 30天内新增的花色卡片显示绿色"🆕 新款"标签（图片右上角 + meta行）
- **同步确认弹窗**: 更新文案为"只新增不覆盖"
- **SQL**: 新增 `add_created_at_column.sql` 确保 pattern_assets 表有 created_at 字段
- **待执行**: 用户需在 Supabase SQL Editor 执行 `add_created_at_column.sql`
- **待提供**: 用户飞书花色素材表格的 table_id（用于配置 FEISHU_PATTERN_TABLE_ID secret）
- commit `7bec0d5`，已 push，Edge Function 已部署

## 花色图/上眼图支持飞书附件字段

- **需求**: 用户不想手动生成图片URL，希望直接在飞书表格上传图片附件
- **Edge Function 改造**:
  - 新增 `extractFileToken()` 从飞书字段值提取附件 file_token
  - 新增 `downloadFeishuFile()` 调用飞书 API 下载附件文件
  - 新增 `uploadToStorage()` 上传到 Supabase Storage 并返回公开URL
  - 新增 `processPatternAttachments()` 遍历记录下载上传图片
  - `mapPatternRecord()` 同时支持附件字段("花色图"/"上眼图")和文本URL字段("花色图URL"/"上眼图URL")
  - SyncType 新增 `preUpsertFn` 在 upsert 前异步处理附件
- **Storage**: 新增 `create_pattern_images_bucket.sql` 创建 `pattern-images` 公开 bucket
- **同步流程**: 飞书附件 → 下载到 Edge Function → 上传到 Supabase Storage → 存储公开URL到 pattern_assets
- **待执行**: 用户需执行 `create_pattern_images_bucket.sql` 和 `add_created_at_column.sql`
- commit `d0d2fd3`，已 push，Edge Function 已部署

### 花色素材新增基弧和定轴参数
- **SQL**: `add_base_curve_fixed_axis.sql` — pattern_assets 新增 `base_curve` text 和 `fixed_axis` text 字段
- **前端筛选**: 基弧筛选行（数据驱动，类似直径，值如 8.6/8.7/8.8）+ 定轴筛选行（全部/定轴/非定轴，类似状态筛选）
- **筛选层级**: 品牌 > 抛型 > 直径 > **基弧** > 系列 > 色系 > **定轴** > 状态
- **卡片显示**: 基弧值显示为 spec tag（"基弧 8.6"），定轴值显示为高亮 spec tag（"定轴 90°"）+ meta-row 定轴标签
- **编辑器**: 新增基弧和定轴输入框，savePattern 保存 `base_curve` 和 `fixed_axis` 字段
- **Edge Function**: mapPatternRecord optionalFields 新增 ["base_curve", "基弧"] 和 ["fixed_axis", "定轴"]
- **定轴逻辑**: fixed_axis 有值 = 定轴款，空值 = 非定轴；筛选"定轴"显示所有有值的花色
- commit `0afe34b`，已 push，Edge Function 已部署
- **待执行**: 用户需执行 `add_base_curve_fixed_axis.sql`
