# 2026-08-16 工作日志

### 2026-08-16 10:08 连带成交：极速登记按钮前置
**用户需求**：客服反馈页面装不下时，「➕ 极速登记」在工具栏最右侧，得往下拉一下才能点到，希望挪到最前面。
**改动**：`index.html` 中 `cs-toolbar-right` 内将「➕ 极速登记」按钮调整到第一顺位（按日排名、历史月份、批量粘贴之前）。
**验证**：内联 JS 语法 0 错误。
**部署**：已 commit `9ab2ac7` 并 push 到 GitHub Pages。

---

### 2026-08-16 10:30 新建「宝宝学习工作台」kids-learn.html
**用户需求**：给一年级升二年级宝宝做学习工作台，薄弱项：语文阅读、数学解决问题、拼音（声母/韵母记不清）、英语零基础；要练习+闯关+积分奖励；手机平板都能打开。
**实现（单文件 kids-learn.html，无后端，GitHub Pages 托管）**：
- 移动端优先响应式（viewport 锁缩放、大按钮、底部 Tab、safe-area）。
- 4 模块：拼音(4关)/语文阅读(3关)/数学解决问题(4关)/英语启蒙(3关)。
- 拼音：声母23/单复前后鼻韵母24/整体认读16 学习表 + 练习（分类、给汉字选声母/韵母、听音选拼音）；英语：A-Z 点读发声+例词、基础单词(颜色/动物/数字)、听音选。
- 数学用模板随机生成应用题（10/20以内加减、图文、两步），题量随关卡增长，无限练习。
- 积分系统：答对+5分、通关+20分+得星、解锁下一关；奖励墙家长可增删奖励、宝宝兑换（扣分清单）。进度/积分存 localStorage。
- 听音用浏览器 `speechSynthesis`（zh-CN / en-US）。
**验证**：内联JS语法0错误；Node+DOM 桩跑通全部模块/关卡题库生成（声母23/韵母24/整体认读16，ALL OK）。
**部署**：已 commit `64a3d4a` 并 push。访问地址：`https://yangru2026.github.io/kefu-workbench/kids-learn.html`
**备注**：目前题库为起步量（拼音汉字14个、阅读3篇、英语单词12个），可后续扩充；奖励默认4项可家长自定义。

---

### 2026-08-16 10:21 改名：学习工作台 → 张依诺的学习乐园
**用户需求**：不要叫"工作台"，学生又不工作，改成"张依诺学习"之类的名字。
**改动**：`kids-learn.html` 全部去掉"工作台"字样，改成"学习乐园"；默认名字从"宝贝"改为"张依诺"；title 改为「🌟 张依诺的学习乐园」；顶部 show 名改为"张依诺<small>学习乐园</small>"；改名弹窗文案改为"给宝贝起个名字吧"。
**验证**：内联JS语法0错误；全文件已无"工作台"残留。
**部署**：已 commit `555bd63` 并 push。访问地址不变：`https://yangru2026.github.io/kefu-workbench/kids-learn.html` 原 `kids-learn.html` 文件名保留（避免线上链接失效）。

---

### 2026-08-16 10:26 学习乐园五大更新（kids-learn.html）
**用户反馈**：①拼音/英语点击发音按钮都不响；②只有4关就结束；③数学“解决问题”应是读题解题的应用题不是算式；④题库素材太少；⑤奖励孩子自己就能拿到，不用家长授权。
**改动（commit `36b675e` 已 push）**：
1. **发音修复**：`speak()` 改为预加载 `getVoices()`、按语言挑 voice、修复 Chrome `cancel` 后立刻 `speak` 被一起取消的 bug（延迟60ms），不支持时提示。
2. **关卡增多**：MODULES max 提至 拼音6/阅读6/数学6/英语5；生成器对高关卡用 `else` 兜底最难题型，不再“4关就结束”。
3. **数学改应用题**：`genMath` 全部改为带情境的汉字应用题（L1 10以内/L2 20以内/L3 两步/L4 比较/L5 倍数等分/L6 混合），每题可🔊听题。
4. **题库扩充**：汉字拼音 28→58 个（拼音参考页去掉 slice(0,12) 限制全展示）；阅读短文 3→9 篇各3题；英语单词 12→56 个（颜色/动物/数字/食物/家人/身体/自然）。
5. **奖励家长授权**：兑换改为“申请兑换→待家长确认→家长 PIN 门禁面板批准/拒绝”。孩子点“申请兑换”先预留积分进 pending；家长从「家长入口」(默认密码1234，可改) 批准才正式到账，拒绝/孩子撤回则退回积分。增删奖励、改密码均在家长面板。孩子无法自行获得奖励。
**验证**：Node 桩跑通全部模块×关卡（198题，0错误）；汉字58/阅读9/英语56；整段脚本语法0错误。

---

### 2026-08-16 11:xx 学习乐园大改（kids-learn.html，commit `04f3610` 已 push）
**用户二次反馈**：①数学题还是太简单（靠猜也能过）；②微信里打开链接点发音提示"设备不支持"；③视觉改成更适合小孩子；④奖励改成临时机制（不固定事项，随用随建"约定"，如"做X需30积分"）；⑤内容太少一会就学完；⑥工作流：先想思路→用户确认再做。
**经 AskUserQuestion 确认方案**：微信打开（重点做无语音兜底）/ 大幅扩充+新增2模块 / 高关卡改孩子自己输入数字。
**改动（commit `04f3610`）**：
1. **微信发音兜底**：`TTS_OK = ('speechSynthesis' in window)` 检测；无语音时 `speak()` 静默返回不报错，顶部显示横幅"在浏览器打开"/题目区 `.notts` 显示"请爸妈读"文字。取代旧版弹 toast 报错。
2. **数学变难+手写**：L1-2 仍四选一(10/20以内)；L3+ 改 `q.type==='input'` 孩子手写数字作答（数字键盘 `inputmode="numeric"`），杜绝瞎猜；L3 两步/L4 排队比多少/L5 倍数等分人民币角/L6 混合两步等量代换。
3. **奖励改临时约定**：去掉固定 `rewards`/`pending`/`redeemed`，改 `state.goals[]`（status: todo/done/approved）。家长建约定→孩子"我完成啦"→done→家长批准→approved 并发分。孩子不能自取。
4. **题库大幅扩充**：汉字拼音 58→131、新增词语拼音47、阅读 9→20篇(分级{1:5,2:8,3:7})、英语单词 56→97(12主题)、新增句子20、反义词20、量词12。
5. **新增两模块**：`mental` 口算速算（60秒计时挑战，max:0 不闯关）+ `words` 词语积累（反义词/量词，max:4）；CORE_ORDER/MORE_ORDER 分组。
6. **儿童化视觉+附加功能**：🦊 mascot、彩虹渐变、圆角大按钮3D按压阴影、🔥连续打卡 pill、过关 `confetti()` 撒花、错题本(`state.wrongs[]`+错题本入口)、连续打卡 `updateStreak()`、护眼提醒(>15分钟)。STORE_KEY 改 `kids_learn_v2`，kidName'张依诺'，parentPin'1234'。
**验证**：
- 语法校验：1 script block OK。
- 生成器冒烟（ktest.js）：汉字131/词语47/阅读20篇/英语97/句子20/反义20/量词12；生成题量241，input 50 / choice 191，ALL OK。
- DOM 桩集成（kitest.js，修复桩后）：数学L3整局得分、约定"建→完成→批准+30分"、四模块各跑一局 INTEGRATION ALL OK。
**部署**：commit `04f3610`，660 insertions / 425 deletions，已 push。访问地址不变：`https://yangru2026.github.io/kefu-workbench/kids-learn.html`

---

### 2026-08-16 11:10 花色素材：开放花色名称编辑权限
**用户需求**：截图反馈编辑花色时「花色名称」显示只读，希望改成可编辑。
**改动**：`index.html` 中 `renderEditorForm` 函数：去掉编辑模式下花色名称后的 `<span style="color:#e17055;">(只读)</span>` 标签，并移除 input 的 `readonly` 属性。保存逻辑 `savePattern()` 已读取 `ef-name` 并通过 `supabase.from('pattern_assets').update(data).eq('id', b._id)` 更新，无需额外修改。
**验证**：内联 JS 语法校验 3 script blocks OK；git diff 仅 2 行改动。
**部署**：commit `963f6c2`，已 push。访问地址：`https://yangru2026.github.io/kefu-workbench/`

---

### 2026-08-16 11:12 花色素材：卡片参数去重整理 + 手动排序/置顶
**用户需求**：截图反馈卡片参数重复显示（如直径/着色重复）且太杂乱；需要手动排序/置顶某些花色。
**改动（commit `36cdba5`）**：
1. **参数展示优化**：`renderPatterns` 中把卡片参数拆成主/次两行。主行显示：抛型、色系、直径（合并"直径X·着色Y"）、基弧、定轴、状态标签；次行显示：材质、透氧量、含水量、规格。去掉了原先直径、定轴在主行和 `specsHtml` 中重复出现的问题。
2. **手动排序/置顶**：
   - 新增 SQL 迁移文件 `add_sort_order_to_pattern_assets.sql`，为 `pattern_assets` 增加 `sort_order` 整数字段（默认 0，越大越靠前）。
   - `buildPatternTree` 映射 `sort_order` → `sortOrder`。
   - `loadPatternsFromDB` 改为按 `sort_order DESC → created_at DESC → name ASC` 排序。
   - `renderPatterns` 同样按 sortOrder 降序优先，其次 created_at，最后 name。
   - 编辑弹窗 `renderEditorForm` 增加「排序权重」数字输入框 + 「🔝 置顶」复选框（勾选自动设为 100，取消置 0）。
   - `savePattern()` 保存时写入 `sort_order`。
   - `openPatternEditor()` 把原记录的 `sortOrder` 传入编辑态。
**验证**：内联 JS 语法 3 blocks OK；Node 纯逻辑测试 sortOrder > createdAt > name 排序结果正确（100 > 50 > 新 > 旧）。
**数据库操作**：需手动在 Supabase SQL Editor 执行 `add_sort_order_to_pattern_assets.sql`。
**部署**：commit `36cdba5`，已 push。访问地址：`https://yangru2026.github.io/kefu-workbench/`

---

### 2026-08-16 11:25 花色素材：下架花色默认排到最后
**用户需求**：所有标记下架的花色默认排序到最后面。
**改动（commit `1b6cf77`）**：`renderPatterns` 排序函数最前面加一层判断：`isDiscontinued` 的花色 `da/db=1` 永远排后面（`if (da !== db) return da - db;`）。`loadPatternsFromDB` 的 DB 排序也加 `.order('is_discontinued', { ascending: true })`（Postgres 中 false<true，下架置底）。
**验证**：内联 JS 语法 3 blocks OK；Node 逻辑测试确认——即使下架花色 sort_order 很高（200/100），仍排在在售花色（0/50）之后。
**部署**：commit `1b6cf77`，已 push。

---

### 2026-08-16 13:45 汇报模块：日报改「汇报」+ 新增周报（A/B/C 三组不同模板，店铺后台可改并同步）
**用户需求**：① 侧边栏「日报」改名为「汇报」，原日报页面不变；② 新增「周报」，每周一汇报，A/B/C 三组模版不同（附截图）；③ 周报要汇报的店铺需后台可改，改后同步到对应组模版。
**新增数据表（需手动在 Supabase SQL Editor 执行 `create_weekly_report_tables.sql`）**：
- `weekly_templates`（group_name UNIQUE + config JSONB）：config = { members[], shops[], common[{key,label,unit,target}], shopTargetDefault, shopTargets{店名:目标} }。店铺指标（转化率-<店名>）由 shops 自动生成，改 shops 即同步指标列。
- `weekly_reports`（user_id+week_start UNIQUE）：content = { rows[{name,values{指标key:值}}], shops[], notes }。
**前端改动**：
- `index.html`：nav「日报」→「汇报」；`page-daily` 内加日报/周报 Tab（`switchReportTab` 新函数，照 `switchPresaleTab` 模式）；周报 Tab 含填写/提交情况/结果卡片三区；模板管理页新增「📊 周报模板」区块（`weekly-templates-content`）。
- `app.js`：新增整套周报逻辑——`loadWeeklyTemplates`/`getWeeklyTemplate`/`getLastWeekRange`/`openWeeklyForm`/`renderWeeklyForm`/`submitWeekly`/`renderWeeklyResult`/`renderWeeklyTrack`/`showWeeklyDetail`/`copyWeeklyResult`/`renderWeeklyReportCanvas`（原生 canvas 截图，沿用日报绕过 html2canvas 思路）/ 后台 `renderWeeklyTemplates`+`saveWeeklyTemplate`+`addWtCommon`+`subscribeWeeklyTemplates`。周报填写按「成员×指标」表格渲染（A组含快手/天猫店铺指标，B组6个拼多多店，C组抖音2店），达标绿色/未达标红色。默认周期为上周一~上周日。
- 接入页面切换钩子：进 templates 页加载并渲染周报模板、订阅实时；进 daily 页加载周报历史。
**时区修复**：`getLastWeekRange` 原用 `toISOString()`（UTC）在 GMT+8 下少一天，改为本地 `getFullYear/Month/Date` 格式化，周期显示正确（今天周日→上周 2026-08-03~08-09）。
**验证**：内联 JS 3 blocks 语法 OK；`node --check app.js` OK；Node 纯逻辑测试确认上周周期与模板归一化（成员/店铺指标合并）正确。
**数据库操作**：需手动执行 `create_weekly_report_tables.sql`（含 A/B/C 三组的默认模板种子数据）。
**部署**：已 commit `3d9b0a0` 并 push。

---

### 2026-08-16 14:00 周报 SQL 修复：CREATE POLICY 语法 + C 组 key 统一
**问题**：用户在 Supabase SQL Editor 执行 `create_weekly_report_tables.sql` 时报错 `syntax error at or near "NOT"`，LINE 25 的 `CREATE POLICY IF NOT EXISTS` 不被 PostgreSQL 支持；截图中 C 组 `overall_conversation` 也实际应为 `overall_conversion`。
**改动**：将 4 处 `CREATE POLICY IF NOT EXISTS` 改为 `DROP POLICY IF EXISTS ... ON ...; CREATE POLICY ...`（先删除已存在再创建，可重复执行、兼容 Supabase）。同时确认 C 组 key 为 `overall_conversion`。
**验证**：重新 Read 文件确认 policy 语法为 `DROP POLICY IF EXISTS` + `CREATE POLICY`，C 组 key 正确。
**部署**：已 commit `e93207a` 并 push。

