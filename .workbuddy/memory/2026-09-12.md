# 2026-09-12

## 连带成交入口改为「舆情转接登记」（commit 72f3c8a，已 push）
- 背景：分流号曾被滥用，现规范化——只允许两种场景转分流号（截图制度里的第 1 种"早晨离线分流"是系统自动，无需登记），登记用于监管。
- 新页面 `diversion.html`（iframe 接入，菜单「🔄 舆情转接」替代原「🛒 连带成交」）：
  - 登记卡：客服名字（从 profiles real_name 自动预填，可改）、客户昵称/订单号、转接原因标签二选一（💧舆情问透氧 / 🏪跨店问正品）、聊天截图**必传≥1张**（多选 + Ctrl+V 粘贴，paste 监听仅拦图片）。
  - 列表：时间倒序 + 搜索（客服/昵称/订单号/原因）+ 截图缩略图点击灯箱（‹›切换+计数）。
  - 权限：登录可查可登记（insert），删除仅 admin；RLS 见 SQL。
  - localStorage 缓存 `kefu_cache_diversion`（v1）秒显。
- 新表 `diversion_transfers`：id/cs_name/customer_info/reason/screenshots text[]/created_at；bucket `diversion-images`（公开读、登录传、admin 删）。**茹姐需在 Supabase SQL Editor 跑一次 `create_diversion_transfers.sql`。**
- index.html 改动点（套路复用）：nav data-page cross-sales→diversion；`#page-diversion.active{display:flex;height:100vh;overflow:hidden}`（iframe 塌陷坑）；switchPage 懒加载分支；缓存参数 v=20260912a。
- **旧连带成交未删**：页面 DOM/JS 保留（只是导航不再指向），cross_sales 表数据原封不动——以后要恢复/查历史随时可以。
- 验证：node 内联语法 0 失败；`outputs/smoke-diversion.js`（离线 mock + Edge）PASS——预填/选原因/传图/提交 insert 字段正确/表单重置/搜索/灯箱/空值拦截全过。
- **坑（冒烟侧）**：mock 存根的 `getSession` 返回对象多写一个 `}` → 存根自身 SyntaxError → 页面 `createClient` TypeError + 后续 eval 全 TDZ。表现像页面坏了，实际是测试脚手架问题；先 `node --check` 存根再怀疑页面。
- 注：index.html 的 SUPABASE_KEY 已被换成新版 `sb_publishable_` 格式（中间某次提交 eef9078），gift/diversion 仍用旧 JWT anon key（线上正常）——两代 key 并存有效。

## 舆情转接列表加表头（commit dde40ba）
- 茹姐实测登记成功后反馈「登记明细需要表头」→ 列表加 `.rec-head`（时间/客服/客户昵称·订单号/转接原因/截图/操作），与记录行同宽对齐 + 底部分隔线；操作列仅 admin 显示；<=600px 表头隐藏（移动端行会换行，表头对不齐）。
- 已验证：语法 0 失败、smoke-diversion 复跑 PASS；缓存参数 v=20260912b。

## 舆情转接加「接待店铺」「分流号昵称」（commit d9846bf）
- 表单新增两必填项：接待店铺（文本框 + datalist 常用 9 店提示：抖音1店/天猫弥生/京东弥生/天猫极氧/抖音极氧/抖音YH/拼多多YH/快手弥生/小红书YH，可自由输入）、分流号昵称（select 下拉：可可/莓莓/睛彩顾问）。
- 列表表头+记录行加「接待店铺」「分流号」两列；搜索支持店铺/分流号。
- 迁移 SQL `add_diversion_shop_nick.sql`（shop/diversion_nick 两列，幂等，茹姐需 SQL Editor 跑一次）。
- 冒烟更新（insert 断言 shop/nick）PASS；缓存参数 v=20260912c。

## 接待店铺名单更新（commit 936aee8）
- 茹姐给定正式名单（替换我预填的旧名单）：抖音1-4店、拼多多1-5店、天猫弥生、天猫极氧，共 11 家。缓存参数 v=20260912d。
- 注意：这是舆情转接页自己的店铺口径，和工作台其他模块（排班/连带成交）的店铺口径不同，别混用。

## 培训资料大类去掉「全部」（commit c81267f）
- 需求：茹姐截图指培训资料大类行的「全部」标签要去掉。
- 改动：renderTrainingGroupTabs 资料分支不再渲染「全部」，并做归一化——trainingGroup 为空/为「全部」/为已删除大类时自动回落到第一个大类；setTrainingModule 切回「资料」时默认第一个大类。renderTraining 里的「全部」聚合分支成为不可达死代码（保留兜底）。
- 小类行的「全部」保留（大类内筛选用）。
- 新冒烟 outputs/smoke-training-no-all.js PASS（无「全部」tab、默认落第一个大类、切大类、兜底归一化）。

## 培训资料小类行也去掉「全部」（commit c386711）
- 小类 tab 行不再渲染「全部」，进入大类默认选第一个小类；没有小类的大类（如操作类/ERP类没建小类时）整行隐藏、内容按该大类全部展示。
- 注意：极氧小类有专门的整页展示分支（renderTraining 里 trainingSubcatFilter==='极氧' 特判，渲染 .jy-page-header 而非普通卡片），测试断言别用卡片数判断。
- 冒烟扩展 PASS。坑：page.evaluate 的返回值没有 {ok} 包装，断言别写成 r.ok。
