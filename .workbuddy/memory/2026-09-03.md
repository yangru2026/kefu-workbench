# 2026-09-03 工作日志

## markDone/markPending 直接更新状态，彻底避免清空图片
- **问题**：标记「已讲解」后记录图片消失。之前版本在 `markDone`/`markPending` 里调用 `syncRecord(r)`，即使加了 `keepSceneImages` 保留字段，仍存在某些情况下图片字段被清空的风险。
- **修复**：`markDone`/`markPending` 改为直接调用 `supabase.from('qc_records').update({status, coach_date, coach_note}).eq('id', id)`，只更新状态字段，**完全不碰 `scene_images/reply_images/knowledge_images`**。
- **影响**：
  - 新建/编辑记录仍走 `syncRecord`，逻辑不变；
  - 标记已讲解/退回待讲解再也不会清空或影响已有图片；
  - 内存中 `records` 对应对象用 `Object.assign(r, updates)` 同步刷新。
- **版本**：`qc-v2.html?v=20260903a`（commit `f4fc87c`）。
- **验证**：inline script 语法 0 错；已 push main。
- **用户操作**：工作台首页 Ctrl+Shift+R 强制刷新后生效。
- **遗留数据**：此前被旧代码清空的图片记录，仍需执行 `qc_recover_images.sql`（2026-09-02 生成）来恢复对应关系。未执行的请继续用 Supabase SQL Editor 跑。

## 质检报告页(index.html #page-qc-report)视觉改造（B方案）
- **需求**：用户嫌报告页排版/字体/视觉眼花缭乱，要求标题大字加颜色区分。
- **改动**：
  - 主标题「质检报告」加大加粗(28px/800) + 区间提示变色。
  - 统计卡片按语义着色：记录数(靛蓝)/扣罚(红)/待讲解(橙)/已讲解(绿)/完成率(青)，左侧色条。
  - 二级标题加大字号 + 颜色块：质检员明细(蓝)/按质检人(绿)/按客服(紫)/按店铺(橙)/按严重程度(红)。
  - 表格边框精简为仅横线，表头去灰底。
  - 质检员明细改为 `<details>` 可折叠分组（箭头动画）。
  - 控制区分两排：时间范围(本周/本月/上月/按月份/自定义) + 操作(更新时间/刷新/导出)。
- **验证**：无头 Edge 冒烟 0 真实 JS 错误；DOM 结构(5 彩色标题 + 两排控制区 + 卡片语义 class)全部就位。

## 导出 Excel 图片嵌入修复（qc-v2.html，工作台 iframe 主用版本）
- **关键 bug**：原 `downloadXLSXWithImages` 里 worksheet 引用了 `rId2` drawing，但 `xl/_rels/workbook.xml.rels` 只声明了 `rId1`(worksheet)，**缺 rId2(drawing)** → Excel 打开图片关系缺失，图片不显示/异常（"导出没图片/占两格"根因）。已补全 rId2。
- **改动**：重写 `exportXLSX` + `downloadXLSXWithImages`：
  - 每字段(客户问题/客服回复/知识点)最多导出 3 张图，每图独立成一列（截图1/2/3），超出在第 3 格备注"共X张"。
  - 图片等比缩放至最大边 70px，锚定单格(oneCellAnchor, 偏移 30000EMU)，**不跨格、不占满**；图片列宽 15、行高 56pt 容纳。
  - 图片下载失败静默跳过并计数（toast 显示"含 N 张配图"）。
  - 新增 `fetchImageBytes` / `getImageDims` 辅助（复用 qc-v2 已修好的 `getImageUrl` 绝对 URL）。
- **注意**：用户日常通过工作台 iframe 用 `qc-v2.html`（非独立 `qc.html`）；`qc.html` 也同步改了同样逻辑。iframe 版本号已升 `20260903b`。
- **验证**：inline script 语法 0 错；qc-v2 导出链无头冒烟执行成功（生成 Excel，无导出相关错误）。
- **旧记录图片**：图片字段在 DB 为空的旧记录，仍需先跑 `qc_recover_images.sql` 才能导出图（代码已正确，数据缺失是历史 bug 所致）。

## 一对一质检反馈页(qc-share.html)图片 404 修复
- **问题**：客服打开反馈链接看不到图片。`qc-share.html` 的 `getImageUrl` 没像 `qc-v2.html` 一样补齐 `/storage/v1` 前缀，`createSignedUrl` 返回的相对路径被解析成当前 GitHub Pages 域名，导致 404。
- **修复**：
  - `getImageUrl` 重写为与 `qc-v2.html` 一致：相对签名 URL 自动补成 `SUPABASE_URL + '/storage/v1' + signedPath`。
  - 签名失败时回退 `getPublicUrl`；只缓存/返回真正的 http(s) 绝对地址。
  - 新增 `imageUrlCache` 避免反馈页重复请求签名 URL。
- **验证**：inline script 语法 0 错；无头 Edge 冒烟确认返回地址为 `https://ienmejlxukhrxjjxvfqf.supabase.co/storage/v1/object/...` 绝对 URL。
- **部署**：commit `51a2281`，已 push main，GitHub Pages 约 1-2 分钟后生效。
- **用户操作**：把反馈链接复制到浏览器重新打开即可；如果记录仍完全没图（连图片占位区都没有），说明该记录在 DB 里图片数组已被旧 bug 清空，需要跑 `qc_recover_images.sql` 恢复数据。

## 新增空图记录自动匹配恢复 SQL
- **问题**：用户跑诊断 SQL 后发现 9 月 2 号还有 7 条记录图片数组为空（小猫 3 条、小七 4 条），原 31 条恢复 SQL 未覆盖到。
- **生成**：
  - `qc_recover_images_auto.sql`：按 `qc_records.created_at` 与 `storage.objects.created_at` 最近 10 分钟匹配孤儿图片，输出待恢复的 UPDATE 语句（含 id/staff/匹配数量），供人工核对。
  - `qc_recover_images_auto_exec.sql`：直接合并成一段可执行 SQL，一键恢复所有匹配到的空图记录。
- **安全护栏**：只匹配未被任何记录引用的孤儿图片；只更新 `scene_images` 为空的记录；每个 id 最多恢复 3 张。
- **提交**：commit `2b48adc`。

## 质检记录图片链接快照备份机制
- **问题**：用户担心以后图片再被 bug 清空，希望有 Excel 级别的「安全感」。
- **实现**：
  - 新建表 `qc_image_snapshots`（`add_qc_image_snapshots.sql`），记录每次操作前的 `scene_images/reply_images/knowledge_images`。
  - `qc-v2.html` 的 `markDone` / `markPending` / `editRecord` / `deleteRecord` 操作前自动调用 `saveImageSnapshot()` 保存快照。
  - 启用 RLS：全员可读，仅 admin/leader 可写。
- **效果**：以后任何状态/编辑/删除操作都会先留一份图片链接快照；若再出现清空 bug，可直接从快照表恢复，无需再匹配孤儿文件。
- **部署**：commit `8d32f4e`，iframe 版本号升至 `20260903c`。

## 质检表格视图改版 + 按角色重做权限按钮（qc-v2.html）
- **需求**：① 表格视图要直接看到图片（可点放大），不要只显示「共N张」；② 表格要显示「问题表述」和「正确做法」；③ 权限分层——管理员/质检员不显示是否讲解/修改/删除按钮，点整行/整卡即进编辑；普通客服不显示、不能改，只能点「确认收到」。
- **表格视图改动**：
  - 截图列由「共N张」文字改为直接渲染图片缩略图（最多 6 张，点击 `previewImage` 灯箱放大）；点图放大用 `stopPropagation` 防止误触整行编辑。
  - 新增「正确做法」列，与「问题表述」(原问题描述)并列显示。
  - 权限用 `canManage = isAdmin`（admin/leader/授权手机）判定。
- **权限模型**：
  - `canManage` 真：卡片/表格整行 `onclick=editRecord` 进入编辑；列表不再渲染是否讲解/修改/删除按钮；是否讲解在编辑弹窗的「状态」下拉改，删除按钮已补进编辑弹窗底部（`deleteEditingRecord`）。
  - `canManage` 假（普通客服）：行/卡不可点击编辑；表格「确认」列与卡片操作区只显示「确认收到」按钮，调用 `confirmReceived` 写入 `received_at/received_by`。
- **新增字段**：`qc_records.received_at`(timestamptz) + `received_by`(text)，SQL=`add_qc_received_fields.sql`（ALTER TABLE IF NOT EXISTS，可重复执行）。
- **验证**：inline script 语法 0 错；无头 Edge 冒烟 0 真实 JS 错误。
- **部署**：commit `0eb4c17`，iframe 版本升 `20260903e`，已 push main。
- **用户操作**：① 先去 Supabase SQL Editor 执行 `add_qc_received_fields.sql`（否则点「确认收到」会提示字段不存在）；② 工作台首页 Ctrl+Shift+R 强制刷新。

## 质检分析配图 + 一键发送 + 深链直达（Request B）
- **质检分析配图**：`renderAnalysisSheet` 改 async，时间线最近10条每条渲染 场景/回复/知识点 缩略图，点击 `previewImage` 灯箱放大；新增 `analysisThumbsHtml` 辅助函数；`renderAllStaffSummary` 同步改 async。
- **一键发送（本人+负责人）**：分析弹窗/讲解单弹窗 footer 各加「📤 发送(本人+负责人)」按钮，仅管理员可见。点击后站内信通知该客服本人（复用 `qcNotifyStaff`）及全部管理员/组长（新增 `qcNotifyAdmins` 查 profiles role in admin/leader），并生成深链。
- **深链直达**：`qc-v2.html?share=analysis|coach&staff=<姓名>` 自动打开对应弹窗并选中客服；`handleDeepLink()` 在 `loadRecords` 末尾及 DEMO init 中调用。
- **清空 9/1-9/2**：生成 `clear_qc_0901_0902.sql`，先 SELECT 核对 → 删 storage.objects 物理文件 → 删 qc_shares(BIGINT[] 包含匹配) → 删 qc_records 级联 qc_images/qc_image_snapshots。待用户在 Supabase SQL Editor 手动执行。
- **验证**：inline script 语法 0 错；无头 Edge 冒烟通过（?demo）：分析时间线含 `timeline-imgs`、两处发送按钮可见、深链解析无错；唯一控制台 404 为 favicon.ico，无影响。
- **部署**：commit `bab1ef9`，index.html iframe 版本升 `20260904a`，已 push main。

## 修复：loadQcImagesInto 不再清空无 qc_images 关联行的旧图片
- **问题**：杨茹反馈「质检分析没有图」。根因是 `loadQcImagesInto` 把没有 `qc_images` 关联行的记录的图片数组强制覆盖成 `[]`，导致历史存在 `qc_records` 旧数组的图片被清空，时间线/列表都看不到图。
- **修复**：qc_images 表有对应行才覆盖；没有时保留 `records` 里已有的旧数组，兼容历史数据。
- **部署**：commit `9d334ad`，iframe 版本升 `20260904b`，已 push main。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强制刷新后生效；若 9/1-9/2 记录本身就没上传图，时间线仍无图，这是正常情况（不是 bug）。

## 质检工具「完全打破重做」决定（2026-09-03）
- 用户决定：连数据一起彻底清空 + 前端完全重写（不受旧代码影响），但**核心需求必须保留**。
- 已生成 `reset_qc_all.sql`：先自动备份所有 qc_* 表到 `qc_backup_20260903` schema（可回退），再 DROP 全部 qc_* 表 + 清空 qc-images 存储桶，重建需另跑建表脚本。
- 用户选「你定，怎么清爽怎么来」→ 最终明确「完全打破重做，不要受之前影响，但需求要记得」。
- 去掉的冗余（造成『乱七八糟』的根源）：旧版自动按上传时间匹配图片、suspect/orphan 待核对、图片修复中心(imgFix)、各类 recover 脚本。
- 新建表脚本：`create_qc_schema_clean.sql`（单一文件，qc_images 去掉 trust 列，不再有 suspect/orphan/claim）。

### 📌 杨茹的质检工具【核心需求清单】（重写时必须逐条落实）
1. **图片绝不丢失**：每条记录 3 类配图——客户问题截图(scene)/客服回复截图(reply)/知识点配图(knowledge)，均支持多张。
   - 架构保证：qc_images 表用 record_id 外键 ON DELETE CASCADE 绑定；文件路径自带记录编号 `rec/<id>/<kind>-<时间戳>-<随机>.jpg`；任何状态变更前自动存图片快照(qc_image_snapshots)；标记已讲解/退回只改状态不碰图片数组。
2. **列表视图**（卡片 + 表格两种）必须**直接显示图片缩略图**，点击**灯箱放大**；长图放大不损清晰度（加载原图 + 200%缩放 + 拖拽 + 滚动）。
3. **权限分层**：管理员/质检员(admin/leader/授权手机)——点整行/整卡即进编辑，列表不显示是否讲解/修改/删除按钮（在编辑弹窗里改/删）；普通客服——只读，只能点「确认收到」。
4. **一对一讲解单（已讲解）**：按客服出单，带配图，「讲完勾选」批量标记已讲解；可**一键发送**（本人=该客服 + 负责人=管理员/组长），附深链直达。
5. **质检分析**：按客服出报告——KPI、近30天对比、问题分类分布、严重程度分布、扣罚汇总、重复问题检测、时间线(最近10条，**带配图**)；可**一键发送**（本人+负责人），附深链直达。
6. **发送质检记录**：生成反馈链接(qc-share.html)，一对一反馈页**带配图可放大**；站内信通知本人+负责人。
7. **深链直达**：`qc-v2.html?share=analysis|coach&staff=<姓名>` 自动打开对应报告/讲解单并选中客服。
8. **Excel 导出**：质检记录/分析可导出（含配图更佳）。
9. **筛选/统计**：按客服/店铺/分类/状态/日期/月份筛选；分页/加载更多。
10. 部署：GitHub Pages 单文件；改动后升 iframe 版本号强刷缓存；Supabase 项目 `ienmejlxukhrxjjxvfqf`。

## 完成：完全重做已交付（commit fa677d6，已 push main）
- **前端**：`qc-v2.html` 从零重写（去掉 suspect/orphan/图片修复中心/recover 等冗余）；`index.html` iframe 版本升 `20260904c`。
- **SQL**：`create_qc_schema_clean.sql`（单一干净建表脚本，IF NOT EXISTS，含 qc_image_snapshots 但前端未强制回填——FK CASCADE + 路径自带 id + 状态变更只改状态不碰图片数组，已足够保证图片不丢）；`reset_qc_all.sql`（先备份 `qc_backup_20260903` 可回退，再清空 qc_* 表 + 存储桶，指向单一重建脚本）。
- **验证**：inline 语法 0 错；无头 Edge 冒烟(DEMO) SMOKE_OK——列表 3 行 3 缩略图、分析时间线 2 图、讲解单 1 图、两处发送按钮可见、深链 `?share=analysis&staff=示例客服A` 直达正确；唯一控制台 404 为 favicon，无真实 JS 错误。
- **⚠️ 杨茹需做（必须，否则前端是空的）**：
  1. Supabase SQL Editor 跑 `reset_qc_all.sql`（备份→清空，可回退）；
  2. 同 Editor 再跑 `create_qc_schema_clean.sql`（重建干净结构）；
  3. 工作台首页 `Ctrl+Shift+R` 强刷。
- **一处刻意简化（已确认清爽优先）**：主列表对普通客服不再单独放「确认收到」按钮——确认收到改由「一对一反馈链接(qc-share.html)」承载（qc_shares.confirmed_at 机制）。若杨茹想要主列表内也保留，再补。
- **排查经验固化**：无头冒烟用本地 `http.server`(后台任务常驻) + `puppeteer-core`(绝对路径 require) + Edge headless；favicon 404 用 `response` 监听器(带 URL)过滤，比 console 文案可靠。

## 修复：灯箱点开图模糊 + 新图上传 file 丢失（commit a282455）
- **问题**：杨茹反馈「这个图点开太糊了」。根因是灯箱默认按 `min(宽度比, 高度比)` 把整张图塞进屏幕，长截图被压成很窄很小，文字糊；同时 `max-width:100%` 限制了放大/zoom 不生效。
- **修复**：
  - 灯箱默认改为「适应宽度」(`imgFitWidth`)：以 wrapper 宽度为上限，长截图上下滚动、文字保持可读。
  - 移除 `#imgPreviewSrc` 的 `max-width/max-height` 约束，zoom/宽度调整真正生效；增加 `image-rendering: crisp-edges` 优化缩放清晰度。
  - 保留「适应屏幕」按钮(`imgFitScreen`)，点一下整图入屏。
- **额外修复**：`onPickFiles` 没有把原始 `File` 对象传给 `addPreview`，导致 `saveEdit` 里 `pendingUploads[kind].file` 始终为 `null`，**新上传的图片根本没传到 Supabase Storage**。已把 `file` 传进去并保存。
- **验证**：inline 语法 0 错；无头 Edge 冒烟 SMOKE_OK。
- **部署**：commit `a282455`，iframe 版本升 `20260904d`，已 push main。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷。

## 质检记录表单改造：客服/店铺下拉 + 移除场景/回复 + 知识点预设（commit 6261f8e）
- **需求**：杨茹反馈「客服姓名和店铺不能勾选；场景和客服回复不需要；只需要问题表述和正确做法；知识点后续需要预设」。
- **改动**：
  - `qc-v2.html` 新增/编辑弹窗中，**客服姓名**改为 `<select>` 下拉，选项来源：`profiles` 表（name/real_name/phone）+ 历史记录 + 当前用户。
  - **店铺**改为 `<select>` 下拉，默认预设 18 个店铺（抖音1-4店、拼多多1-6店、淘宝旗舰店、天猫、小红书、京东、有赞、YH、弥生、极氧），合并历史记录去重排序。
  - 移除「场景」「客服回复」两个文本框；对应截图上传（客户问题截图/客服回复截图）保留。
  - 知识点改为 `<input list="e_knowledge_list">`，内置 10 个常用预设（发货时效SOP、催单话术、美瞳参数科普、退换货流程、度数换算、售后补偿标准、活动规则、新手引导话术、品牌区分、退换货运费说明），可自由输入也可选预设。
  - 修复 `afterLoad` 里 `new Set(...).filter(Boolean)` 误用导致 `Set.filter is not a function` 的 JS 报错。
- **验证**：inline 语法 0 错；无头 Edge 冒烟 SMOKE_OK；编辑弹窗专项断言 EDIT_OK——客服/店铺为 SELECT、场景/回复不存在、问题表述/正确做法保留、知识点为 INPUT+datalist、店铺选项 19 个。
- **部署**：commit `6261f8e`，iframe 版本升 `20260904e`，已 push main。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷。

## 基础数据管理页：客服/店铺可在线增删（commit b8c1660）
- **需求**：杨茹问「客服名单和店铺明细我需要改，在哪可以改」→ 选择「做个『基础数据』管理页」（自维护，不碰代码）。
- **新增 SQL**：`create_qc_options.sql` —— 新建 `qc_options` 表（category TEXT['staff'|'shop'] + name TEXT，唯一约束 (category,name)）；开启 RLS；策略：登录用户可读写（增删是否开放由前端 `isAdmin` 控制，沿用 qc_records 的「内部工具」pattern）。
- **前端改动 `qc-v2.html`**：
  - 顶部工具栏新增「⚙️ 基础数据」按钮（`id=btnSettings`，仅管理员可见，由 `updateAdminUI` 控制显隐）。
  - 新增 `qc_options` 读写逻辑：`loadMetaOptionsFromDB()`（从 `profiles` 取客服名 + 从 `qc_options` 取 staff/shop，首次加载自动把默认 18 店铺 seed 进表）、`addOption(cat)`、`deleteOption(cat,name)`、`openSettingsModal()`/`renderSettingsList()`。
  - `init()` 改为 `await loadMetaOptionsFromDB()`（修复上一轮遗留的 `loadStaffOptionsFromProfiles is not a function` 报错——该函数已被替换）。
  - 管理弹窗展示客服/店铺标签，可 × 删除、可输入新增；删除只动 `qc_options`，不动已有质检记录。
- **验证**：inline 语法 0 错；无头 Edge 冒烟 SMOKE_OK（列表/分析/讲解/深链全通过）；新增 SETTINGS_OK 专项（按钮可见、弹窗打开、staffTags=2/shopTags=18、空输入提示「请输入名称」、DEMO 守卫「演示模式不可保存」、下拉 HTML 生成正确、无真实 JS 错误）。
- **部署**：commit `b8c1660`，iframe 版本升 `20260904e`→`20260904f`，已 push main（`1893e7f..b8c1660`）。
- **⚠️ 杨茹需做（必须，否则基础数据页读不到表会提示去跑 SQL）**：
  1. Supabase SQL Editor 跑新增的 `create_qc_options.sql`（建表 + 策略，幂等可重跑）；
  2. 工作台首页 `Ctrl+Shift+R` 强刷；
  3. 点工具栏「⚙️ 基础数据」即可增删客服/店铺（首次会自动把 18 个默认店铺写进表）。

## 新增/编辑弹窗彩色化（commit 1f8b4dc）
- **需求**：杨茹截图反馈「登记页面太死板，所有选项都用色块颜色区分，下拉勾选的每个选项都标注不同的颜色标签」。
- **改动 `qc-v2.html`**：
  - **分类（可多选）**：5 个分类各自固定配色（话术问题橙 / 响应问题蓝 / 流程问题紫 / 态度问题红 / 产品知识绿），未选中是浅色底+彩色字，选中后反白。
  - **严重程度**：由普通下拉改为 3 个彩色 pill（一般黄 / 严重红 / 严重需整改紫），点选即生效。
  - **状态**（编辑时）：改为彩色 pill（待讲解橙 / 已讲解绿）。
  - **客服姓名 / 店铺**：由原生 `<select>` 改为自定义彩色下拉组件 `color-select`，每个选项前带彩色圆点，选项颜色按名称哈希稳定生成；触发框显示当前选中项+彩色圆点。
  - **知识点**：保留输入框，下方新增 10 个彩色预设小 chip，点一下即可填入输入框。
  - 新增配色辅助函数 `nameColor()` / `hashHue()`、`tagChipHtml()` / `sevPillsHtml()` / `statusPillsHtml()` / `knowledgePresetsHtml()` / `colorSelectHtml()`。
- **验证**：inline 语法 0 错；无头 Edge 冒烟 SMOKE_OK（核心列表/分析/讲解/深链未受影响）；SETTINGS_OK 专项更新为验证彩色下拉组件 + 彩色 pill + 彩色标签存在；生成 `outputs/edit-modal-colorful.png` 预览弹窗效果。
- **部署**：commit `1f8b4dc`，iframe 版本升 `20260904f`→`20260904g`，已 push main（`b8c1660..1f8b4dc`）。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷即可看到彩色弹窗。

## 截图区支持 Ctrl+V 粘贴 + 客户问题截图置顶 + 移除客服回复截图（commit 9c67232）
- **需求**：杨茹反馈「放截图的地方不能粘贴吗」+「把截图放到登记第一步，只需要客户问题截图，不需要客服回复截图」。
- **改动 `qc-v2.html`**：
  - 新增 `handlePaste()` / `addPreviewFromFile()`，截图上传区支持 **Ctrl+V 粘贴**剪贴板图片；上传区改为虚线框，提示「点击选择图片，或在此按 Ctrl+V 粘贴截图」。
  - 登记表单字段顺序调整：**客户问题截图**放到第一步（最上方）。
  - 移除「客服回复截图」字段及上传区；保留「知识点配图」。
  - 新增 `outputs/smoke-edit.js` 断言登记表单布局（第一步是客户问题截图、无客服回复截图、知识点配图保留、粘贴区存在）。
- **验证**：inline 语法 0 错；SMOKE_OK / EDIT_OK / SETTINGS_OK 全通过。
- **部署**：commit `9c67232`，iframe 版本 `20260904g` 未变（当时未 bump），已 push main。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷。

## 基础数据页支持批量粘贴客服 / 店铺（commit 9fcdcf0）
- **需求**：杨茹截图反馈「客服店铺管理这里需要可以批量粘贴，自动识别」。
- **改动 `qc-v2.html`**：
  - 「⚙️ 基础数据」弹窗中，客服名单 / 店铺明细的添加区由单行输入框改为**多行文本框**，支持批量粘贴。
  - 新增 `parseOptionNames()`：按换行 / 逗号 / 空格 / 制表符自动拆分并去重。
  - 新增 `parseOptionPreview()`：点「🔍 自动识别」后展示识别出的彩色预览标签，先确认再入库。
  - 新增 `addBatchOptions()`：点「＋ 确认添加」把未重复的名称批量写入 `qc_options`；已存在的自动跳过。
  - 保留原 `addOption()` 单条添加能力（兼容旧调用）。
- **验证**：inline 语法 0 错；SMOKE_OK / EDIT_OK / SETTINGS_OK 全通过；SETTINGS_OK 新增断言批量识别出 4 条店铺；生成 `outputs/settings-batch-paste.png` 预览效果。
- **部署**：commit `9fcdcf0`，iframe 版本升 `20260904g`→`20260904h`，已 push main（`1f8b4dc..9fcdcf0`）。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷 → 点「⚙️ 基础数据」→ 在文本框里粘贴一列客服/店铺 → 自动识别 → 确认添加。

## 发送质检清单含图 + 客服自主确认（模式一/二，commit c33a1a6）
- **需求**：杨茹追问「发送质检清单含图、客服自主确认这两个功能没有做吗」→ 确认要做两种模式：① 质检专员/管理员发送含图清单；② 普通客服在列表对自己记录点「本人确认已知晓」。
- **方案确认**：通过 AskUserQuestion 确认三处推荐选项：
  1. 自确认数据用新建 `qc_confirmations` 表；
  2. 客服在列表直接点确认；
  3. 质检清单含图复用现有 `qc-share.html` 一对一反馈链接（已带图）。
- **新增 SQL**：`create_qc_confirmations.sql` —— 建 `qc_confirmations` 表（record_id FK CASCADE、staff、user_id、confirmed_at），启用 RLS，策略为 authenticated 可读写（前端限制为本人 = 记录所属客服）。
- **前端改动 `qc-v2.html`**：
  - 工具栏按钮由「📤 发送记录」改为「📤 发送质检清单（含图）」；发送弹窗标题改为「发送质检清单（一对一反馈，含图）」。
  - 新增 `qcConfirmations` 状态、`loadConfirmations()`（启动后异步加载确认记录）、`getConfirm()`/`getConfirms()`/`fmtConfirmTime()`。
  - 新增 `confirmControlHtml(r)`：
    - 若当前登录客服是记录所属客服且未确认 → 显示绿色「本人确认已知晓」按钮；
    - 若已确认 → 显示「✅ 本人已确认 时间」；
    - 若管理员视角 → 显示该记录下所有已确认客服名单或「未确认」。
  - 新增 `confirmRecord(id)`：调用 Supabase 写入 `qc_confirmations`，成功后重新渲染列表并提示「本人已确认：已知晓该质检问题」。
  - 列表表格新增「确认」列；卡片视图在底部显示确认按钮/状态。
  - 新增 `outputs/smoke-confirm.js` 断言：表格有确认列、本人记录显示按钮、非本人不显示、已确认显示 badge、管理员视角显示已确认。
- **验证**：inline 语法 0 错；SMOKE_OK / EDIT_OK / SETTINGS_OK / CONFIRM_OK 全通过；生成 `outputs/list-confirm-column.png` 预览客服视角的确认列效果。
- **部署**：commit `c33a1a6`，iframe 版本升 `20260904h`→`20260904i`，已 push main（`9fcdcf0..c33a1a6`）。
- **⚠️ 杨茹需做（必须，否则客服点确认会报错）**：
  1. Supabase SQL Editor 跑新增的 `create_qc_confirmations.sql`（建表 + 策略，幂等可重跑）；
  2. 工作台首页 `Ctrl+Shift+R` 强刷；
  3. 质检员：勾选记录 → 点「发送质检清单（含图）」→ 生成链接并站内通知对应客服；
  4. 普通客服：登录后在工作台看到只读列表，找到自己名下的记录 → 点「本人确认已知晓」。

## 客服下拉去杂（排除管理员本人）+ 必填校验 + 连续登记记忆（commit 待定）
- **需求**：杨茹反馈「新增记录时客服下拉出现模版里没有的名字（如杨茹本人）」+「常用选项（客服/店铺）下次登记要保留上次选择」+「设必填项，不填无法保存」。
- **改动 `qc-v2.html`**：
  - `populateMetaOptions()` 改为：**客服/店铺下拉优先取自「基础数据」(`qc_options`)**；基础数据为空时退回 `profiles + 历史记录`，但**始终排除当前管理员本人**（`currentAuthUser.name/phone`，即杨茹）— 彻底避免下拉混入无关账号。
  - 新增 `lastStaff` / `lastShop` 状态：`openEditModal` 在「新增」时把默认客服/店铺预填为上一次保存的值（`editingId==null ? lastStaff : ''`），实现**连续质检登记记忆**。
  - `openEditModal` 给 客服姓名 / 店铺 / 日期 / 问题表述 四个标签加红色 `*` 必填标记（`req()` 辅助）。
  - `saveEdit` 增加必填校验：客服姓名 / 店铺 / 日期 / 问题表述 任一为空 → `showToast` 报错并 `return`，**无法保存**；校验通过后把本次选择写入 `lastStaff`/`lastShop`。
- **验证**：inline 语法 0 错；SMOKE_OK / EDIT_OK / SETTINGS_OK / CONFIRM_OK 全通过（之前因 `populateMetaOptions` 误删历史 fallback 导致 SETTINGS_FAIL，已修复——恢复 fallback 且排除管理员本人，DEMO staffTags 恢复为 2）。
- **部署**：commit 待定，iframe 版本升 `20260904i`→`20260904j`，已 push main。
- **用户操作**：工作台首页 `Ctrl+Shift+R` 强刷 → 新增记录时下拉只剩「基础数据」里的客服（杨茹不再出现）、常选项保留上次选择、四个必填项不填无法保存。
- **说明**：若「基础数据」(`qc_options`) 里已维护客服名单，下拉就只显示那些名字（最干净）；若还没维护，会退回历史记录里的客服名（仍不含杨茹）作为兜底，方便先用起来。建议杨茹在「⚙️ 基础数据」里把真实客服名单补全，下拉就会完全等于自己的模版。

