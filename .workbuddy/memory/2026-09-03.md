# 2026-09-03 工作日志

## markDone/markPending 直接更新状态，彻底避免清空图片
- **问题**：标记「已讲解」后记录图片消失。之前版本在 `markDone`/`markPending` 里调用 `syncRecord(r)`，即使加了 `keepSceneImages` 保留字段，仍存在某些情况下图片字段被清空的风险。
- **修复**：`markDone`/`markPending` 改为直接调用 `supabase.from('qc_records').update({status, coach_date, coach_note}).eq('id', id)`，只更新状态字段，**完全不碰 `scene_images/reply_images/knowledge_images`**。
- **影响**：
  - 新建/编辑记录仍走 `syncRecord`，逻辑不变；
  - 标记已讲解/退回待讲解再也不会清空或影响已有图片；
  - 内存中 `records` 对应对象用 `Object.assign(r, updates)` 同步刷新。
- **版本**：`qc-v2.html?v=20260903a`（commit `f4fc87c`）。
- **验证**：inline script 语法 0 错；已 push main。
- **用户操作**：工作台首页 Ctrl+Shift+R 强制刷新后生效。
- **遗留数据**：此前被旧代码清空的图片记录，仍需执行 `qc_recover_images.sql`（2026-09-02 生成）来恢复对应关系。未执行的请继续用 Supabase SQL Editor 跑。
