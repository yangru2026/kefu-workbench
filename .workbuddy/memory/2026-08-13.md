# 2026-08-13 工作日志

## 连带成交：始终显示空白行 + 批量粘贴 + 第二单金额导入强化

**用户反馈**：
1. 表格没有「新增行」按钮（页面暂无数据时只显示「暂无连带成交数据」，找不到登记入口）；
2. 历史数据多来自表格直接复制粘贴，不想每次都导出 Excel；
3. 导入 Excel 时第二单金额始终为空。

### 修复与改动

1. **始终显示空白行（index.html）**
   - `renderCrossSales` 当 `rows.length===0` 时仍调用 `renderCrossSalesEditableRows(tbody, [], ...)` 渲染表格。
   - `renderCrossSalesEditableRows` 当没有任何 `data-id` 和 `data-new` 行时，自动在表格头部插入一个空白可编辑行（`createBlankCsRow`）。
   - 重构 `addCrossSalesBlankRows` 复用 `createBlankCsRow`，去掉重复 HTML 模板代码。
   - 效果：客服首次进入页面即便完全没数据，也能直接看到并填写一个空白行。

2. **批量粘贴弹窗（index.html）**
   - 工具栏新增「📋 批量粘贴」按钮（所有登录客服可见）。
   - 新增 `mod-cs-paste` 弹窗，textarea 接收 tab/逗号分隔数据。
   - 列顺序按表头：日期·店铺·成交客服·产品类型·第一单订单号·第二单订单号·第一单状态·第一单金额·第二单金额·合计金额（合计可省）。
   - 智能识别表头（首行含「日期/订单号/金额」等关键字则跳过）。
   - 「🔍 预览」按钮：显示识别条数 + 前 3 条预览 + 错误提示。
   - 「✅ 导入」按钮：解析后批量 upsert 到 `cross_sales`，自动反查客服组别、自动计算合计金额，导入成功跳到最早一条的日期。

3. **第二单金额导入强化（index.html）**
   - 第一单金额 keys 增加：`第一单金额元` `第一单金额(元)` `第一单` `第一笔金额` `first_amount`。
   - 第二单金额 keys 增加：`第二单金额元` `第二单金额(元)` `第二单` `second_amount` `第二笔金额` `连带金额` `二单` `连带销售额`。
   - `getCell` 二级模糊匹配保留字符集改为 `(元|amount|金额|订单)`（兼容带「订单」后缀的列名）。
   - 未匹配时输出 `console.debug` 列出表头和 keys，方便排查。

### 提交

- `b9c9c3c` feat(cross-sales): 默认显示空白行+批量粘贴导入+强化第二单金额字段匹配
- 已 push 到 GitHub，GitHub Pages 自动部署。

## 连带成交三项紧急修复 + 数据安全性增强 (2026-08-13)

**用户反馈（三个问题）**：
1. 按了 Ctrl+F5 后连带登记内容全清空，连同保存的选项管理也消失，担心误删数据；
2. 点击「按日排名」默认跳到 1 号而不是当天（今天 13 号）；
3. 导入 Excel 时「店铺」「成交类型（产品类型）」「第一单状态」列没匹配上。

### 根因（关键！）
- **Ctrl+F5 并不是真的数据丢失**：数据全部存在 Supabase，curl 验证 939 条记录完好。
- 真正原因：Ctrl+F5 清掉浏览器缓存后，jsdelivr CDN 在浏览器内解析失败（`ERR_NAME_NOT_RESOLVED`），supabase 客户端从未初始化，页面就显示空白——看起来像"数据被清空"。
- 附带 bug：`var supabase`（UMD 全局）与创建的客户端同名冲突；UMD 库把全局 `supabase` 设成库对象，原 `if (supabase) return` 守卫误判"已初始化"跳过了 `createClient`，导致 939 条永远加载不出来。

### 修复（commit 5c820a2，已 push）
1. **库本地化 + CDN 回退链**：下载 supabase.min.js (212KB)、xlsx.full.min.js (882KB) 到 `vendor/`，loader 改为 vendor → jsdelivr → unpkg 三级回退，并设 `window.__SUPABASE_LIB_OK` 防止库被重新加载覆盖客户端。
2. **initSupabase 守卫修复**：`if (supabase && typeof supabase.from === 'function') return;` + `waitForSupabaseLib()` 轮询，失败显示红色错误横幅（含刷新链接），不再静默空白。
3. **恢复选项加载**：`switchPage('cross-sales')` 缺失 `loadCrossSalesOptions()` + `subscribeCrossSalesOptions()` 调用，已补上（选项消失根因）。
4. **按日排名默认今天**：`toggleCrossSalesRankMode` 切回日模式时 `crossSalesDate = getTodayStr()`。
5. **Excel 智能列匹配**：新增 `matchImportColumns()` 按同义词打分匹配 + 排除正则（如"第二单订单号"不会抢"第二单金额"列），`店铺/产品类型/第一单状态` 全部正确识别。

### 后续增强（本次提交）
6. **今日无数据自动跳转最近有记录日期**：`loadCrossSalesFromDB` 当今天无任何记录时，自动跳到 `crossSalesData` 中最近的有记录日期，避免"今天 13 号但数据都在 11 号"时页面空白误以为被清空。
7. **友好空状态文案**：`该日期暂无连带成交数据（数据都在云端，不会丢失）` + 提示用「📅 按月份排名」或选其他日期。

### 验证
- Edge headless 两端验证（本地 127.0.0.1:8123 + 线上 GitHub Pages）：`supabaseIsClient: true`、`crossSalesDataLen: 939`、`csOptionsTeams: [default, C组, A组, B组]`、`csOptionsA_shop: 11`。

### 待执行 SQL（历史遗留，与本次无关）
- ~~`create_cross_sales_options.sql`（升级 option_templates 支持 team）~~ ✅ 用户已于 2026-08-13 执行
- ~~`create_cross_sales_archive_months.sql`（归档月份表）~~ ✅ 用户已于 2026-08-13 执行

## 连带成交 · 防并发刷新丢字 + P1 价值增强 (2026-08-13)

**用户反馈**：两个历史 SQL 已执行；要求「把正在编辑的格子暂存」防并发覆盖；并实现 P1（Enter 字段流转 + 常驻「我的今日」迷你看板）。

**实现（index.html）**：
1. **编辑暂存防覆盖**：`captureCSInputState` / `restoreCSInputState` 在 `renderCrossSalesEditableRows` 渲染前暂存脏输入（按 data-id）、渲染后回填，未保存的字在并发刷新/切换筛选后依然在。
2. **编辑中暂缓拉取**：`subscribeCrossSales` 实时订阅在有人登记触发刷新时，若 `csTableIsEditing()`（焦点在 tbody 或有行正在保存）则设 `window._csPendingReload=true` 跳过本次加载；`focusout` + `saveCSRow` 保存成功后补刷。避免打断正在输入的客服。
3. **P1-① Enter 字段流转**：`#cross-sales-tbody` 委托 `keydown` → `csEnterNavigation`：Enter 跳下一可编辑字段；末行末字段（空白新行）自动保存并 `ensureAndFocusBlankRow()` 聚焦新空白行，实现连续录入。移除了 4 处内联 `Enter=blur` 处理器避免冲突。
4. **P1-② 我的今日迷你看板**：页面头部新增 `cs-mytoday-card`（始终常驻），`getMyTodayStats` / `renderCSMyTodayCard` 实时显示今日第二单金额 / 当前排名 / 距上一名差多少 / 登记笔数 + 冲榜提示；在 `renderCrossSales` 末尾调用，随每次数据变化刷新。

**验证**：node 语法检查 0 错误；Edge headless 功能测试——7 个新函数均 function、看板计算正确（小明今日 ¥150 / 第2名 / 距上一名 ¥150 / 2 笔，正确排除 8/12 旧数据）、暂存机制有效（未保存 999 经重渲染后仍为 999）、**0 报错**。已 commit 待 push。

## 连带成交 · 极速登记（让登记比飞书云文档更方便）(2026-08-13)

**用户反馈**：当前连带登记没有飞书云文档方便，希望让客服能快速登记，体现工具价值。

**根因分析**：当前唯一登记入口是 12 列宽表格（min-width 1080px，需横向滚动），无专门快速登记入口；登记后无即时反馈；店铺/产品类型每次重选。而工具本身已具备飞书没有的实时排名、自动合计、重复预警、按组统计——价值没被「用出来」。

**实现（index.html，P0 三件套）**：
1. **极速登记弹窗**（`mod-cs-quick` + `openCsQuickRegister` / `submitCsQuick`）：工具栏新增醒目「➕ 极速登记」按钮（所有登录客服可见）。弹窗紧凑表单（日期默认今天、成交客服=当前登录人、店铺/产品类型/第一单状态下拉、第一/第二单订单号、第一/第二单金额、自动合计）。回车即提交；勾选「连续登记」后不清空、光标回到首字段，一条接一条录。
2. **记忆上次选择**（`getRememberedCsSel` / `rememberCsSel`，localStorage 按人存 `cs_last_sel_<uid>`）：弹窗与行内空白行都预选上次店铺/产品类型/第一单状态，减少重复操作。
3. **登记即时反馈**（`showCsSuccess` + `getCSRankForDate`）：提交成功后绿色 toast 显示「已登记 · 你今日第二单 ¥X · 当前排名第 N」，把登记动作与业绩反馈即时连起来（飞书做不到）。

**复用**：插入走 `supabase.from('cross_sales').insert([rec]).select()`（与 `saveCSRow` 同模式，已验证 939 条可正常加载）；校验复用 `validateCSRow`；下拉选项复用 `csOptionHtml`；提交后 `crossSalesData.push` 本地即时刷新 + `renderCrossSales()`，800ms 后 Realtime 自动全量重载（无重复）。

**验证**：node 语法检查 2 段 inline script 0 错误；Edge headless 本地服务器冒烟测试——无 console/pageerror，5 个新函数均为 function，弹窗/按钮/下拉/toast 元素均存在，`openCsQuickRegister()` 调用不抛错。已 commit + push 到 GitHub Pages。

## 连带成交 · 极速登记模板同步 + 一键复制 (2026-08-13)

**用户反馈**：极速登记弹窗里的「店铺 / 产品类型 / 第一单状态」下拉选项和保存的模板不一致；需要一键复制功能把原来的数据复制进来；导入 Excel 时数据总是对不上。

**实现（index.html）**：
1. **模板同步工具栏**：极速登记弹窗顶部新增工具条，显示当前生效模板组别（`模板：A组`），提供：
   - 🔄 同步模板：重新从 Supabase `option_templates` 加载当前组别选项并刷新三个下拉框，解决"看到旧列表"或"和模板不一致"的问题。
   - 📋 复制选项：下拉选择「复制 店铺 / 产品类型 / 第一单状态」，把该组当前所有可选值复制到剪贴板（换行分隔），可直接粘到 Excel/文档做导入模板，确保导入值和系统选项完全一致。
   - ⏪ 复制上一条：读取当前客服最近一条登记的店铺/产品类型/第一单状态并填充到弹窗，快速复用上次选择。
2. **打开弹窗时自动显示组别标签**，便于客服确认当前用的是哪套模板。

**验证**：node 语法检查 0 错误；Edge headless 本地测试——3 个新函数（`syncCsQuickOptions` / `copyCsQuickOptions` / `copyLastCsRecord`）均为 function，工具栏元素（teamLabel/copyOptsSelect/modal）均存在，0 报错。已 commit + push (`4d11b3c`)。

## 连带成交 · 选项管理UI重设计 + 工具栏简洁化 (2026-08-13)

**用户反馈**：选项管理中 A组/B组/C组 没有不同颜色区分；想要像飞书云文档那样每个选项有不同颜色色块；页面按钮太多太杂乱；整体色系要简洁大方。

**实现（index.html）**：
1. **选项管理弹窗重设计**：
   - 组别标签页改为圆角胶囊，每个组别有独立色点：通用（灰）、A组（主色绿）、B组（蓝）、C组（橙）。
   - 选项类型标签（店铺/产品类型/第一单状态）改为更小更干净的胶囊按钮。
   - 选项列表从「表格行」改为「彩色标签云」：每个选项显示为圆角标签，左侧带颜色圆点，标签背景按颜色半透明渲染，悬停显示删除按钮，整体像飞书单元格里的彩色色块。
   - 新增 `hexToRgba` 工具函数把颜色转 rgba 做标签背景。
   - 添加/批量添加区域简化：单条添加为一行紧凑输入（颜色选择器 + 名称 + 添加按钮）；批量添加收进 `<details>` 折叠，减少视觉噪音。
2. **连带成交工具栏简洁化**：
   - 工具栏分左/右两区：左侧日期切换 + 组别筛选；右侧主操作（按月份排名 / 历史月份 / 批量粘贴 / 极速登记）。
   - 把admin/低频操作（选项管理、导入Excel、导出Excel、飞书同步、协作者、归档本月）折叠进「··· 更多」下拉菜单，页面不再拥挤。
   - 导出范围默认收起，点击「导出Excel」后展开为日期范围选择条。
   - 新增 `toggleCsMoreMenu` / `closeCsMoreMenu` / `toggleCsExportArea` 函数控制下拉与导出区。
3. **整体色系**：保持现有鼠尾草绿主色，减少高饱和紫色/蓝色杂色；卡片、标签、按钮统一用圆角、浅边框、低饱和背景，视觉上更简洁大方。

**验证**：node 语法检查 0 错误；Edge headless 本地测试——选项管理渲染出彩色标签云（2 个测试选项均正确渲染为 `.cs-opt-pill`），4 个组标签、3 个类型标签、工具栏、更多下拉、导出区域均存在，0 报错。已 commit + push (`c40a5b3`)。

## 连带成交 · 新增「清空全部成交明细」(2026-08-13)

**用户需求**：把已存起来的成交明细（包括云端）全部清空，准备重新用批量粘贴测试能否和原表格对上。

**关键约束（重要）**：`cross_sales` 表启用 RLS，删除仅限 `admin/leader`（策略 `管理员可管理连带成交数据` FOR ALL USING EXISTS profiles.role IN admin/leader）。本沙箱只有 publishable(anon) key，无 admin JWT / service_role key，脚本化云端删除会被 RLS 拦截或静默失败。因此**不能由我在沙箱直接清空云端**，必须由用户在已登录的管理员会话中触发。

**实现（index.html，commit 6f3f5a8 已 push）**：
- 「··· 更多」下拉新增 `🗑 清空全部成交明细`（class `admin-only-nav` 仅管理员可见，红色 danger 样式）。
- 新增 `clearAllCrossSales()`：二次 `confirm` 强确认 → 分页（`range` 每 1000 条）拉取所有 id → `.delete().in('id', ids)` 批量删 → toast 显示共删除条数 → `loadCrossSalesFromDB()` 刷新视图。只删 `cross_sales` 明细，不动选项配置与历史归档。
- 注意：app 不从 localStorage 缓存 cross_sales，清空云端后刷新即空白，无独立"本地"数据需额外清。

**验证**：node 语法检查 0 错误；Edge headless 本地测试——`typeof clearAllCrossSales === 'function'`、`#cross-sales-clear-all-btn` 存在、0 page/console 错误。

**用户操作路径**：登录管理员 → 连带成交页 → 「··· 更多」→「🗑 清空全部成交明细」→ 二次确认 → 云端清空 → 再用「📋 批量粘贴」测试。

## 连带成交 · 下拉框自动配色 + 批量粘贴去重 (2026-08-13)

**用户反馈**：
1. 表格中「店铺 / 产品类型 / 第一单状态」下拉框不同选项值没有不同颜色区分（截图显示全白/统一色）。
2. 批量粘贴导入时报错：`ON CONFLICT DO UPDATE command cannot affect row a second time`。

**根因与修复（index.html，commit ac8a4d6 已 push）**：
1. **颜色不显示**：`applySelectColor` 逻辑已存在，但很多历史选项在 `option_templates` 里存的是纯字符串或对象缺 `color` 字段，导致 `opt.dataset.color` 为空，select 保持白底。
   - 新增 `csColorForValue(value)`：根据选项值字符串哈希从 20 色板中生成稳定颜色（同值同色）。
   - 新增 `csNormalizeOption(item)`：把字符串/对象统一转成 `{value, color}`，缺 color 时自动补色。
   - `loadCrossSalesOptions` 加载后对所有选项做 `csNormalizeOption` 规范化（仅内存，不写库）。
   - `renderCsOptionsList` 与 `addCsOption` 也复用该规范化/补色逻辑，保证选项管理页与表格下拉颜色一致。
2. **粘贴 upsert 失败**：粘贴数据中存在重复键（同一天 + 同一组第一/第二单订单号），PostgreSQL 单次 upsert 里同一条冲突记录不能更新两次。
   - `submitCsPaste` 在 upsert 前按冲突键去重，保留最后一条，统计去重条数。
   - `previewCsPaste` 预览时检测并提示重复组数。
   - 导入成功 toast 显示「已导入 X 条（已去重 Y 条）」。

**验证**：node 语法检查 0 错误；Edge headless 本地测试——`csColorForValue`、`csNormalizeOption`、`submitCsPaste`、`previewCsPaste` 均为 function，0 page/console 错误。已 push 到 GitHub Pages。

## 连带成交 · 批量粘贴改为「全量预览+高亮+可删」（不再静默去重） (2026-08-13)

**用户反馈**：上一版的自动去重是"拦截"行为——重复行被静默丢弃，用户看不到、也无法自行决定删哪条。要求：不要拦截，正常显示所有行，重复的行高亮标出，用户复核后自己删。

**修复（index.html，commit 2f72339 已 push）**：
1. `previewCsPaste` 重写为全量渲染：列出**所有**粘贴行（不去重、不丢弃），按冲突键（日期+第一单号+第二单号）识别重复组，重复行整行加 `.cs-dup-row`（橙色背景 + 左侧琥珀色竖条）高亮。
2. 预览表格每行尾部加「✕ 删除」按钮，调用 `removeCsPasteRow(idx)` 从 `window._csPasteRows` 删除并重渲染（用户复核后自行删冗余项）。
3. 拆分出 `renderCsPastePreview(errors)`，供预览与删除共用；`window._csPasteRows` 保存当前预览结果，删除/导入都基于它。
4. `submitCsPaste` 改用 `window._csPasteRows`（反映用户删除）；**仅当导入集合内仍有残留重复时**才弹 `confirm` 询问（合并为一组/返回删除），不再静默丢数据；toast 文案改为「合并了 N 条重复」。
5. 修了一个连带 bug：`parseCsPasteText` 表头检测原为"命中任一关键字"，导致数据值含「店铺」被误判为表头、吞掉第一行数据；改为"命中 ≥2 个关键字"才算表头。

**验证**：node 语法检查 0 错误；Edge headless——① 无表头纯数据 5 行/2 重复/删除后 4 行；② 带表头 3 行/2 重复，均正确；0 page/console 错误。已 push 到 GitHub Pages。

## 连带成交 · 下拉框当前值不在模板时自动兜底显示 (2026-08-13)

**用户反馈**：批量粘贴导入后，表格中「店铺 / 产品类型 / 第一单状态」全部显示"请选择"，值没有匹配上。

**根因**：`csOptionHtml` 生成 select 选项时只从 `option_templates` 模板里取值。如果 Excel 里的实际值（如"淘宝官方旗舰店"）不在已保存的模板选项中，select 里就没有对应 option，`select.value = 当前值` 会匹配失败，最终显示"请选择"。数据本身已正确写入数据库，只是前端下拉框渲染丢了值。

**修复（index.html，commit b31c429 已 push）**：
- 修改 `csOptionHtml(key, currentVal, team)`：生成模板选项前，先判断 `currentVal` 是否已存在于模板列表中；若不存在且非空，则自动插入一个兜底 `<option>`（自动分配颜色、带 `selected`）。
- 模板中存在的值仍走原逻辑正常匹配和配色；模板中没有的值也能正常显示、选中、保存。

**验证**：node 语法检查 0 错误；Edge headless——模板无"淘宝官方旗舰店"时，`csOptionHtml('shop','淘宝官方旗舰店','A组')` 输出包含 `<option value="淘宝官方旗舰店" selected ...>`，select.value 正确；模板内已知值"天猫"也正确选中；0 page/console 错误。已 push 到 GitHub Pages。

## 连带成交 · 排名默认改为按月 (2026-08-13)

**用户要求**：排名当前默认按日，需要默认按月；只有点击切换按钮时才切到按日（或其他）。

**修复（index.html，commit 0c74739 已 push）**：
1. 默认值 `let crossSalesRankMode = 'day'` → 改为 `'month'`（第 3002 行）。
2. `loadCrossSalesFromDB` 初始化 `crossSalesDate` 时，按当前模式决定：月模式取 `今天.slice(0,7)+'-01'`（当月第一天），日模式取今天；并保留「当前周期无记录则自动跳到最近有数据的日期/月份」逻辑（按月时用月份前缀匹配）。
3. 切换按钮初始文案 HTML 由「📅 按月份排名」改为「📅 按日排名」（与默认月模式一致，点一下切到日）。`toggleCrossSalesRankMode`、改日期/上下日/粘贴导入后/Excel导入后/选历史月等处的模式设置保持不变（均为用户明确交互）。

**验证**：node 语法检查 0 错误；Edge headless 调用 `renderCrossSales()` 后，`#cross-sales-month-rank-btn` 文案为「📅 按日排名」（即当前默认月模式），0 page/console 错误。已 push 到 GitHub Pages。

## 连带成交 · 性能优化 + 一键定位重复项 (2026-08-13)

**用户要求**：工作台和连带成交页很卡，要优化；要能一键定位重复项并高亮出来；整体简洁方便。

**卡顿根因 & 修复（index.html，commit 74dbc94 已 push）**：
1. **全量重渲染**：月视图几百行×每行3个 `<select>`，每次 render 重建全部 DOM。→ 改为**分页**（每页 `CS_PAGE_SIZE=60` 行），仅渲染当前页 + 合计行 + 控件行 + 分页行；「加载更多」**追加**下一页（不破坏已渲染行与编辑态）。新增 `csRowHtml()` 复用行模板；`loadMoreCsRows()` / `updateCsPager()` 处理追加与分页文案。
2. **查重 O(行×数据)**：`scanCSDuplicates`/`checkCSOrderDuplicate` 每行都 `crossSalesData.find`。→ 新增 `buildCSOrderNoIndex()`（加载后/渲染前构建 `date|orderNo → 次数` 索引，O(N) 一次），查重改为 O(1)，仍保留「当天订单号重复红框」提示。
3. **选项 HTML 每行重复拼接**：`csOptionHtml` 加 `_csOptHtmlCache` 缓存（按 key|team|value），同值不重复构建。
4. **重复渲染两次**：`loadCrossSalesFromDB` 与 `loadCsArchivedMonths` 各 render 一次。→ 改为加载完归档月份后**统一渲染一次**（原 `loadCsArchivedMonths` 改名 `reloadCsArchivedMonths`，仅归档/解档后调用）。

**一键定位重复项**：
- 「··· 更多」新增 `🔍 定位重复项`（`locateCSDuplicates()`）：按唯一键（日期+第一单+第二单订单号）扫描当前明细，`computeCSDupGroups()` 统计；高亮重复行（`.cs-dup-row` 红底红条 + `.cs-dup-badge` 🔴重复角标），顶部横幅显示「N 组重复（M 条多余）」并**滚动到第一条**。
- 横幅提供「🗑 删除多余（每组留首条）」(`deleteCsDupExtras()`，管理员，分批 delete+confirm) 与「✕ 取消定位」(`clearCsDupLocate()`)。定位模式下展开全部行确保高亮可见。

**验证**：node 语法 0 错误；Edge headless——
- 分页：130 条 → 首页 60 行 + 分页文案「已显示 60 / 共 130」✅；加载更多追加至 120/180 ✅（修复了 append 用 `<div>` 容器导致 `<tr>` 被浏览器丢弃的 bug，改用语义 `<tbody>` 临时容器）。
- 重复高亮：`csLocateDup=true` 时 130 条中 4 条重复全部标红 + 4 个角标；`csLocateDup=false` 后清零 ✅。
- 0 page/console 错误（仅 2 条 ERR_NAME_NOT_RESOLVED 为沙箱离线资源，非代码问题）。
- ⚠️ 注意：测试中发现真实云端 `cross_sales` 已有 939 条数据，月视图现仅渲染 60/页，性能应明显改善。

## 工作台全局卡顿根治 + 定位重复项改为「仅显示重复项」筛选 (2026-08-13 续)

**用户反馈**：刷新后仍很卡（首页、花色素材尤其明显），数据导入后更卡；要求「定位重复项」点击后只显示重复项、其他隐藏，恢复用「取消筛选重复项」，且筛选仅个人页面、不影响他人。

**全局卡顿根因**：各表 Realtime 订阅（`subscribePatterns/Ranking/Presale/Schedule/Training/...`）在**任意一行变动**时都立刻 `loadXxxFromDB()` 全量重拉+整页重渲染。批量导入 130 行 → 触发 130 次 Realtime 事件 → 130 次全量重拉（除 cross-sales 已有 800ms 防抖外，其余全部无防抖）。主线程被反复全量重拉阻塞，导致含首页在内的全站卡顿。

**修复（index.html，待 push）**：
1. 新增通用 `debouncedReload(key, fn, 700ms)`：把短时间内的多次变动合并为一次全量重拉，导入风暴 130 次 → 1 次。
2. **所有订阅统一防抖**：`subscribePatterns/Training/TrainingCategories/PatternCategories/Schedule/Ranking/PresaleMonthly/Requests` 回调全部改为 `debouncedReload(...)`。
3. **仅当前页才刷新（重量级页）**：`subscribePatterns`、`subscribePatternCategories`、`subscribeCrossSales` 增加 `if (currentPage !== 'xxx') return;` 守卫——非本页时忽略变动，不再后台全量重拉 357 条花色 / 939 条成交。
4. **进入页强制刷新保新鲜**：`switchPage` 的 `patterns` 与 `cross-sales` 分支改为每次进入都 `loadPatternsFromDB()` / `loadCrossSalesFromDB()`（配合上述守卫，既避免后台浪费、又保证打开即最新）。
5. **启动减负**：`onSupabaseReady` 移除 `loadCrossSalesFromDB()`（939 条改为首次进入连带成交页才拉），加快首屏。

**定位重复项 → 本地筛选模式（不影响他人）**：
- 「··· 更多」按钮文案 `🔍 定位重复项` → `🔍 筛选重复项`。
- `renderCrossSales` 在 `computeCSDupGroups(rows)` 之后，若 `csLocateDup` 为真，对 `rows` 做 `filter`（仅保留 `_csDupGroupMap[key] > 1` 的重复行），**其他行从 DOM 中移除**。
- 横幅文案改为「🔍 已筛选出 N 组重复记录（共 M 条，已隐藏其他记录）」，按钮「✕ 取消筛选重复项」（`clearCsDupLocate()`）。无重复时横幅提示「当前筛选范围内没有重复记录」+ 取消按钮。
- `locateCSDuplicates()` 打开筛选并滚动到第一条重复行；`clearCsDupLocate()` 退出筛选（纯前端状态，不写库、不广播，天然仅当前用户视图，不影响他人页面）。管理员仍可「🗑 删除多余」。

**验证**：node 语法 0 错误；Edge headless——
- 新函数 `renderCrossSales/debouncedReload/locateCSDuplicates/clearCsDupLocate` 均存在。
- 筛选逻辑：mock 4 行（3 行同日 + 1 组重复）→ `total=3`、`filtered=2`、`groups=1` ✅。
- 完整 `renderCrossSales()`（`csLocateDup=true`）：DOM 仅渲染重复行 `["1","2"]`（唯一行 id3、其他日期 id4 均隐藏）✅；横幅可见且文案含「取消筛选重复项」✅；0 page/console 错误。