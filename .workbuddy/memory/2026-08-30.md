# 2026-08-30 工作日志

## AI 提问功能（方案确认 + 实现）

### 需求
工作台内嵌 AI 提问：客服遇到难回答的、专业的、或不知怎么组织语言的问题时可直接提问；管理员（杨茹）能看**所有人**的提问与回答记录用于监看；其他成员只能提问、只能看**各自**历史记录，互相看不到，也不知道存在监看。

### 关键约束与判断
- 工作台是纯前端 GitHub Pages，**大模型 API Key 绝不能写进前端**（会被从源码扒走盗刷）。→ 必须走 Supabase Edge Function，Key 存 Supabase Secrets。
- 已有基础可复用，无需新建登录体系：Supabase Auth；`profiles` 表含 `role`(admin/leader/其他)、`name`、`group_name`；`isAdmin` 沿用 `role in ('admin','leader')` 判断；已有 Edge Function 先例 `supabase/functions/feishu-sync/index.ts`。
- 权限必须落在**数据库层（RLS）**而非前端隐藏，这样改前端也绕不过。

### 用户选型（AskUserQuestion 确认）
- 大模型：**豆包**（火山方舟）
- 知识来源：**带上**工作台已有培训资料与话术（轻量检索后再回答）
- 提问次数：**不限制**

### 交付物
1. `create_ai_questions.sql` — `ai_questions` 表 + RLS 策略：
   - insert：仅 `auth.uid() = user_id`（伪造不了身份）
   - select：本人 或 admin/leader
   - update/delete：仅 admin/leader
2. `supabase/functions/ai-ask/index.ts` — 校验 JWT → 以用户身份建 client（RLS 自动生效）→ 用 bigram 关键词在 `training_knowledge` / `training_scripts` 检索 top6 → 调豆包 → 落库（含 user_name/group_name/total_tokens/latency_ms）。
   - 系统提示词内置红线：不承诺医疗效果、不用绝对化用语、眼部不适一律建议停戴就医、不确定就说不确定。
3. `index.html` — 侧栏新增「AI 提问」；新增 `#page-ai`（样式内联在该页内）；`switchPage` 加钩子；JS：`renderAiPage / setAiTab / loadAiHistory / renderAiHistory / submitAiQuestion / copyAiAnswer / formatAiAnswer`。管理员「全部记录」Tab 由 `isAiAdmin()` 控制显隐。

### 豆包 API 要点
- `POST https://ark.cn-beijing.volces.com/api/v3/chat/completions`，OpenAI 兼容格式
- `model` 填**推理接入点 ID**（`ep-` 开头）或模型名（如 `doubao-seed-2-1-pro-260628`）
- 鉴权 `Authorization: Bearer <ARK_API_KEY>`

### 验证
- inline script 语法检查：4 段，0 错误
- DOM 校验：9 个 id + nav 项 + switchPage 钩子全部齐全

### 待用户执行（我无法代做）
1. Supabase SQL Editor 跑 `create_ai_questions.sql`
2. `supabase secrets set ARK_API_KEY=... ARK_MODEL=...`
3. `supabase functions deploy ai-ask`

## 环境备注（可复用）
- 本沙箱 `git clone`/`fetch`（大包传输）会被静默 kill，ssh 鉴权正常但空输出退出 1；小 thin pack 的 push 可通过。
- `curl` https 出站超时（exit 28），**不能**作为部署判据；以「git push 成功 + 本地冒烟」为准。

## 环境核实（重要，影响后续部署方式）
- **supabase CLI 未安装**（`command -v supabase` 为空，不在 PATH）。虽 `supabase/.temp/linked-project.json` 存在（ref `ienmejlxukhrxjjxvfqf`、项目名 `youhu-kefu`、org `jqbhbgqopukvvtxpuygs`），说明历史上有过 CLI 并 link 过，但当前不可用。
- **结论：Edge Function 的部署与 Secrets 配置必须走 Supabase Dashboard 网页方式**，不能直接 `supabase functions deploy`。
- 若后续要恢复命令行（改函数更方便），需重装 CLI 并 `supabase login` + `supabase link --project-ref ienmejlxukhrxjjxvfqf`。
- 用户是非技术用户，指引要写成「点哪里 / 填什么 / 看到什么算成功」，不要只给命令。

## 成员自助邀请 + 离职清退功能（杨茹要“下次不用找我”）

### 需求
管理员在「成员管理」页可自助生成邀请链接+二维码发给新人，新人自己注册后**自动获得**对应角色/组别；离职客服可一键清退（禁用、可恢复），恢复后重新可登录。

### 方案（AskUserQuestion 确认）
- 邀请方式：**生成链接+二维码，新人自己注册**（非管理员代填）
- 清退方式：**禁用账号（可恢复）**

### 交付物（已 commit + push：`58e71a0`，main 自动部署）
- `add_member_invite_rls.sql` — 需杨茹在 Supabase SQL Editor 执行：
  - `pending_invites` 表（token/phone/name/group_name/role/used_at/expires_at）
  - `profiles` 加 `status`(active/disabled) + `left_at`
  - 管理员可改任意成员的 RLS（原只许改自己）
  - `get_invite_by_token(token)` / `claim_invite(token)` 两个 SECURITY DEFINER RPC（避免暴露全表）
- `index.html`：
  - `#page-members` 从空壳补齐：`loadMembers/renderMembers`（列表+角色/状态筛选）、内联改角色组别、清退/恢复、`subscribeProfiles` 实时订阅
  - 邀请弹窗 `openInviteModal/generateInvite`：生成链接+二维码（本地 `vendor/qrcode.min.js`，qrcode-generator 1.4.4，CDN 兜底）+ 复制/撤销；底部「我发出的邀请」列表带二维码
  - `signIn` 改造：登录时查 `profiles.status`，`disabled` 直接 `signOut` 并拒绝
  - 注册页识别 `?invite=token` → `initInviteFromUrl` 预填姓名并提示；注册成功后 `applyInviteClaim` 自动写角色/组别并标记邀请已用
- `app.js`：`doRegister` 邀请场景自动登录触发申领；`initInviteFromUrl` 在 `onSupabaseReady` 调用

### 验证
- `node --check app.js` 通过；index.html 6 段内联脚本 `new Function` 校验 0 错误
- Node 打桩执行成员脚本块：12 函数齐全、`renderMembers` 无异常、`esc` 防注入、`roleText/fmtDate` 正常
- Node 实跑 `qrcode(0,'M')` 生成 SVG 成功

### 待用户执行（我无法代做）
- **Supabase SQL Editor 跑 `add_member_invite_rls.sql`**（否则只有成员列表能看，邀请/清退/改角色会报 RLS/表不存在错误）

## ⚠️ Git 空索引坑（重要，下次必看）
- 本仓库偶发 `git ls-files` 返回 0（索引被清空），但 `git ls-tree HEAD` 仍有全部文件 → `git status` 把所有文件误判为 `D`（已删除）。
- **修复**：先 `git add -A` 用工作树重建索引，再正常 `git commit` + `git push origin main`（本次空索引无需 thin-pack  workaround，常规 push 成功 `022b9f8..58e71a0`）。
- 提交前用 `git status --short | awk '{print $1}' | sort | uniq -c` 确认无残留 `D`（应只剩 A/M）。
- 另有环境备注：supabase CLI 不可用、curl 出站超时不能作为部署判据（见上）。
