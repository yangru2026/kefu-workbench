# 2026-09-04 工作日志

## 修复：新增质检记录报错「null value in column "id" ... not-null constraint」（commit 444c2db）
- **现象**：杨茹截图反馈新增质检记录保存失败，toast 报 `null value in column "id" of relation "qc_records" violates not-null constraint`。
- **排查**：前端 `saveEdit` 的 `rec` 对象**不含 id**，插入时省略 id 本应由数据库自动生成 → 问题在表结构。仓库里两版旧建表脚本 `add_qc_tool.sql` / `ensure_qc_records_full_schema.sql` 把 `qc_records.id` 定义为 `id BIGINT PRIMARY KEY`（**无默认值无自增**）；9/3 重做的 `create_qc_schema_clean.sql` 才是正确的 `GENERATED ALWAYS AS IDENTITY`。结论：线上 qc_records 仍是旧定义（9/3 的 DROP/重建未完全生效或未跑）。
- **修复**：生成 `qc_fix_id_identity.sql`（幂等，可重复执行）：
  1. 查询当前 id 列定义；
  2. DO 块：`is_identity <> 'YES'` 时 `ALTER TABLE qc_records ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY`；
  3. `setval` 把序列拨到 `MAX(id)+1`（防历史数据主键冲突）；
  4. 体检其余 qc_* 表（qc_images/qc_image_snapshots/qc_shares/qc_confirmations/qc_options/qc_staffs/qc_shops）的 id 是否同样缺失自增；
  5. BEGIN/ROLLBACK 干跑验证（不留数据）。
- **需杨茹执行**：Supabase SQL Editor 跑 `qc_fix_id_identity.sql`；第 3 步查询结果若还有其它表 `is_identity=NO 且 column_default 为空`，把表名发来我再补对应 SQL。
- **前端无需改动**（代码本来就不传 id）；无需强刷缓存。
- **执行反馈（杨茹跑完）**：第 4 步干跑报 `null value in column "shop"` —— 这证明 **id 自增修复已生效**（报错行 id=1788422349332759 已自动生成，不再是 null）；新报错是干跑行没带 shop，而线上表 `shop TEXT NOT NULL` 无默认值（与 clean 版 schema 有出入，线上表仍是旧结构）。已把验证 SQL 补上 shop（commit `4ad0008`）。
- **遗留确认项**：线上 qc_records 结构与 `create_qc_schema_clean.sql` 不一致（shop NOT NULL、疑似多余 created_at/updated_at 列），但功能可用；第 3 步其它 qc_* 表的体检结果杨茹尚未反馈，暂未发现其它表报错。

## 功能：灯箱「提取文字」本地 OCR（commit f869f3b）
- **需求**：杨茹问图片打开后能否直接复制图上的文字/订单号；经 AskUserQuestion 选定「灯箱加提取文字按钮（tesseract.js 本地 OCR）」方案。
- **实现**（qc-v2.html，iframe 版本 20260905d→**20260905e**）：
  - 灯箱工具栏新增绿色「🔍 提取文字」按钮；结果面板（底部浮层）含状态/进度条、疑似单号 chips、可框选复制的全文 textarea、「复制全部」。
  - `runImgOcr()`：按需动态注入 `https://cdn.jsdelivr.net/npm/tesseract.js@4.1.1/dist/tesseract.min.js`（带 promise 缓存，避免重复加载）→ 非 data: 图片先 `fetch→Blob→objectURL` 防跨域画布污染 → `Tesseract.recognize(src,'chi_sim+eng',{langPath})` → 语言包默认 `tessdata.projectnaptha.com/4.0.0`，失败自动切备用源 `cdn.jsdelivr.net/gh/naptha/tessdata@gh-pages/4.0.0` 重试一次。
  - 疑似单号正则：`/(?:[A-Za-z]{1,4})?\d{6,24}/g` 去重取 ≤24 个，chip 点一下即复制；`navigator.clipboard` 失败降级 execCommand。
  - `previewImage/closeImgPreview` 均调 `resetOcrUi()`（token 失效 + 清理 blob URL + 复位按钮）。
- **验证**：node 语法检查通过；`outputs/smoke-ocr.js`（Edge headless + stub Tesseract）全链路 OK（按钮→busy→识别→结果→chips→收起/关闭复位，无 console 错误）；perm/todo/todo2 三个旧冒烟全部回归 PASS。
- **提醒杨茹**：首次识别会下载约 10MB 中文识别包，需稍等；识别在本机浏览器进行、图片不上传；若某张图识别不出（如手写/倒置/超长截图），可放大后再试。


## 功能：花色「价格速查」独立页（直径分组 × 价格档，防低价错标）
- **需求**：杨茹要把花色素材另做一个页面：分小直径/大直径，再按价格档（29.9/49.9/59.9/69）分组，防止客服低价订单备注高价花色，便于新人区分；档位以后她自己维护，不用找开发。
- **方案（AskUserQuestion 确认）**：工作台新菜单页 + 编辑弹窗加档位下拉 + 管理员批量打标 + 未打标落「待分组」区。档位先按 29.9/49.9/59.9/69 播种（她选了「自定义输入」但没给具体值，SQL 注释标注了修改区，页面右键也能改）。
- **实现**（index.html，未动 app.js/qc-v2，无需 bump iframe 版本）：
  - 数据层：`pattern_assets` 新增 `price_tier`/`diam_group` 两列；`pattern_categories` 的 CHECK 扩展 `price`/`diam_group` 两类 → 档位/分组走现有右键筛选菜单 + ➕ + 合并对话框即可自维护（级联 fieldMap 已覆盖新字段）。
  - 新页面 `#page-pattern-price`（菜单「💰 花色价格速查」）：搜索 + 显示已下架 + 未打标统计；分组=直径组（含「⏳ 待分组（未标直径）」警示组），组内按价格档分块（含「❓ 未标价格」），卡带价格徽标/🆕/下架划线；管理员显示批量勾选条（价格档+直径分组下拉，`applyPatternBatchTag` 批量 update）。
  - 筛选联动：花色素材页筛选行新增「直径分组」「价格」两行（`patternDiamGroupFilter`/`patternPriceFilter`），编辑弹窗「直径」下方新增价格档/直径分组下拉（`ef-price`/`ef-diamgroup`），`buildPatternTree` 透传 `priceTier`/`diamGroup`。
  - 实时订阅：patterns/pattern-price 两页共用 pattern 资产 + 分类变更刷新。
- **验证**：`outputs/smoke-pp.js`（Edge headless 分步 evaluate + 看门狗）19 项断言全过 `PRICE_OK`、0 真实报错：客服视图分组/档位/下架隐藏/无勾选框、管理员 4 勾选框+批量条、批量打标 payload（69元/副+小直径→pattern_assets in ids）、搜索、编辑弹窗回填、patterns 页筛选函数；node 语法检查 6 个内联 script 全过。预览图 `outputs/pp-page-preview.png`。
- **踩坑记录（smoke 调试）**：① `confirm()` 弹窗在无头浏览器必须注册 `page.on('dialog', d=>d.accept())`，否则 evaluate 永久挂起（此前 smoke-pp SIGTERM 无输出的根因）；② supabase mock 要支持 `.order()` 链式四连 + thenable；③ `.in(col, vals)` mock 别漏列名参数；④ `renderEditorForm` 依赖 `window._editPattern`/`window._editImages` 需先初始化。
- **部署**：commit 5cd5e16，main 已 push。**需杨茹先在 Supabase SQL Editor 执行 `add_pattern_price_tier.sql`**（幂等）：扩 CHECK → 加两列 → 播种 4 档 + 2 直径组 → 按直径自动预填 diam_group（≥14.5 大直径，其余小直径）→ 验证查询。执行后强刷工作台即可见新菜单。

## 价格速查页三项体验优化（杨茹截图反馈：图片慢/不能直接改/排版乱）
- **反馈**：①图片加载超慢；②想在新页面直接改价格档/直径分组，不想跳回花色素材；③排版乱、弥生和极氧混在一起、要翻很久。
- **根因**：价格页一次性渲染全部花色（几百张图同时加载），而素材页有分页（每页 20 + 加载更多）；无品牌维度；单卡只能去编辑弹窗改。
- **修复（AskUserQuestion 三项均选推荐项：品牌页签/卡片直接改/加载更多）**：
  1. 品牌页签 `pp-brand-tabs`（全部/弥生/极氧 + 管理员➕，复用 `.brand-tab` 样式与 `getMergedCats`），`setPpBrand()` 切换并重置分页；
  2. 分页渲染：`ppRenderLimit`/`PP_PAGE_SIZE=20`，截断时底部「加载更多 ↓（还有 X 款）」`ppLoadMore()` 保留滚动位置追加；搜索（`onPpSearch` 防抖 250ms）/显示已下架/切品牌都重置分页；
  3. 卡片快捷改档（管理员）：`.pp-quick` 两个小下拉（价格档/直径分组），`ppQuickSet(id,field,value)` → `update().eq('id')` + 本地 patternData 同步 + 保持滚动位置重渲染，不用跳页；
  4. 普通客服视图不变（无勾选框无快捷下拉）。
- **验证**：smoke-pp.js 扩到 28 项断言全过 `PRICE_OK`（新增：品牌页签过滤、分页 3/5 张+加载更多文案、快捷改档 payload `update().eq('id','a4')`、本地同步、客服无快捷下拉）；语法检查 6 块 inline script 全过；新预览图 `outputs/pp-page-preview.png`。踩坑：汇总 numeric 表漏 key 会静默 FAIL（`>=undefined`）。
- **部署**：commit 6f1be19，main 已 push，无需跑 SQL、无需 bump iframe 版本。

## 价格速查页品牌页签调整（杨茹反馈：去掉全部 + 未选中也要有颜色）
- **改动**：①去掉「全部」页签，进入页面默认选中第一个品牌（`ppBrandFilter` 失效时自动落到第一个品牌）；②页签配色改为 JS 内联双色板（PASTEL 浅底/SOLID 实色），未选中也显示浅底+同色描边，选中为实色白字带阴影；样式加 `#pp-brand-tabs` 作用域，不影响花色素材页全局 `.brand-tab`；未打标统计也随品牌缩小范围。
- **验证**：smoke-pp.js 更新为无全部/默认首品牌行为，26 项断言 + 数值断言全过 `PRICE_OK`（期间排查 admin_checkbox=3：统计点在开「显示已下架」之前，默认弥生视图 3 卡是对的，修正期望值）；语法 6 块全过。
- **部署**：commit 01c5d37，main 已 push，无需 SQL。
git add index.html .workbuddy/memory/2026-09-04.md && git commit -q -m "feat(patterns): 价格速查拆双品牌独立页（常用资料板块：弥生/极氧各一页），移除合并页与品牌页签" && git log --oneline -1 && git push origin main 2>&1 | grep -E "main ->"

## 价格速查拆分为双品牌独立页（杨茹：每个品牌一页，放常用资料，原页面去掉）
- **改动**：①导航：客服常用里的「💰 花色价格速查」删除；「常用资料」板块顶部新增「💰 弥生价格速查」「💰 极氧价格速查」两个入口。②实现：两入口共用同一页面容器（switchPage 别名路由 PP_ALIAS，price-miyang/price-jiyang → ppBrandFilter 锁定品牌 + 标题动态改 + currentPage 仍为 pattern-price，实时订阅/权限/批量/快捷改档/分页逻辑全部复用，零复制）。③页面内品牌页签整体移除（setPpBrand/#pp-brand-tabs/相关 CSS 删除），ppBrandFilter 失效时兜底取第一个品牌。
- **验证**：smoke-pp.js 更新：旧导航已删、两个新入口存在、别名路由切极氧/弥生（标题+内容+导航高亮）、分页/快捷改档/批量等共 27 项断言 + 数值全过 PRICE_OK；语法 6 块全过；预览图更新。
- **注意**：以后新增品牌要加快捷页的话，在 switchPage 的 PP_ALIAS 加一行 + 导航加一个 nav-item 即可。
- **部署**：commit 227fa5c，main 已 push，无需 SQL。

## 两级卡片结构（价格档 → 档内花色）
- 需求：第一层按价格档渲染大卡片（只展示档位名/款数/直径分布徽标），点击进入第二层才见花色（按大/小直径分组）；`ppTierView` 状态变量，`openPpTier()`/`ppBackToTiers()` 两级切换
- 踩坑：`refreshAdminUI`（行 3319）会重设批量条 display，把 renderPatternPricePage 里的隐藏覆盖掉 → 真实入口 switchPage 顺序 render→refreshAdminUI 导致管理员第一层批量条误显示；修复为 `(canEditPatterns && ppTierView)`；冒烟测试已加回归断言 aliasBarHiddenL1
- 测试：smoke-pp.js 按两级结构重写（seed 加 a6/a7 覆盖档内分页+跨档移动），41 布尔断言 + 8 数值断言全过 PRICE_OK；语法 6 块全过
- 预览：screenshot-pp.js 输出两张图（第一层价格档卡片 pp-page-preview.png + 档内花色 pp-tier-preview.png）
- 部署：commit 256ab8f 已推送 main（GitHub Pages 约 1-2 分钟生效，看不了就 Ctrl+Shift+R 强刷）

## 四维度入口（抛型/系列/直径/价格）
- 需求：第一层从「只按价格档」扩成四个分区（⏱按抛型/🎨按系列/⭕按直径/💰按价格），点任意卡片进明细；用户选择「仅独立入口」不做叠加筛选
- 实现：`ppDimView={dim,val}` 替代 ppTierView；`PP_DIMS` 定义四维度（key 取值函数 + badgeOf 卡面徽标维度）；`ppDimOptions(dim,items)` 统一取各维度选项（price/diam 用托管分类，type/series 用托管+已用值）；明细分组规则：按直径进来用价格档分组，其余维度按直径分组
- 踩坑：种子数据 a8 弥生给了 49.9 导致「49.9 只在极氧」断言翻车 → 改 59.9；改种子后 filterFnOk 期望值要从 1 款改 2 款
- 测试：smoke-pp.js 44 布尔 + 11 数值断言全过 PRICE_OK，语法 6 块全过；截图两张（四分区第一层 + 29.9 明细）
- 部署：commit d62d7dc 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 三级结构（分类入口 → 选项卡片 → 花色明细）
- 需求（截图反馈）：第一视觉只看「按抛型/按系列/按直径/按价格」四张大卡，点进去第二层才看该分类的选项卡片（如各系列），再点选项第三层才看花色明细
- 实现：ppDimView={dim,val:null}=第二层选项卡，val 有值=第三层明细；第一层四张 .pp-tiercard.l1 大卡（共 N 类 · M 款花色）；dimCard 提升为函数级复用；第三层返回按钮 ppBackToDimOptions(dim) 回第二层，第二层 ppBackToDims() 回第一层；批量条条件加 `&& ppDimView.val`（仅第三层显示，refreshAdminUI 同步改）
- 踩坑：改层级后旧断言引用选项名（如 49.9元/副）在第一层不存在 → aliasMiyangOk/searchOk 改查层级特征；backToL2Ok 忘了输出数值字段导致缺失断言
- 测试：smoke-pp.js 48 布尔 + 13 数值断言全过 PRICE_OK，语法 6 块全过；截图两张（第一层四张大卡 + 第二层按系列）
- 部署：commit 35f5a34 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 分类大卡居中 + 四色主题
- 需求（截图反馈）：第一层四张分类大卡居中展示，每个分类不同颜色
- 实现：PP_DIMS 增加 color 字段；第一层卡片 inline style `--c:颜色`；CSS 用 var(--c) 统一控制标题、左边条、hover 边框/阴影、进入文字；卡片 `display:flex` 居中 + `text-align:center`
- 测试：smoke-pp.js 61 项断言 PRICE_OK 全过，语法 6 块全过
- 部署：commit dcd1623 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 第一层卡片网格：一行 2 个 + 整体居中
- 需求（截图反馈）：卡片没有水平居中，要求一行 2 个卡片
- 实现：`.pp-tiercards` 从 `auto-fill` 改成 `repeat(2, minmax(0, 320px))` + `justify-content:center`；移动端 (<720px)  fallback 为 1 列
- 测试：smoke-pp.js PRICE_OK，语法 6 块全过
- 部署：commit 9546bd1 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 分类大卡放大占满页面
- 需求（截图反馈）：2×2 居中后卡片太小，要求放大占满页面
- 实现：`.pp-tiercards` 列宽从 `minmax(0,320px)` 改 `minmax(0,1fr)`（撑满内容区）；l1 卡 padding 34/24、min-height 170、标题 23px、副文 15px
- 测试：PRICE_OK + 语法全过
- 部署：commit 5404e75 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 第二层选项卡改自适应多列
- 需求（截图反馈）：系列等数量多的分类页，2 列大卡竖排太长要下拉，一眼看不全
- 实现：第二层选项卡容器加 `pp-opt-grid` 类：`repeat(auto-fill,minmax(230px,1fr))` 宽屏自动 4 列、紧凑字号；第一层保持 2×2 大卡不变
- 测试：smoke-pp.js 新增 optGridOk + optGridCols(≥3) 断言，PRICE_OK，语法全过
- 部署：commit e874253 已推送 main（约 1-2 分钟生效，看不到就 Ctrl+Shift+R 强刷）

## 高价特殊品专区（初音/定轴）——价格速查防发错备注
- **需求**：初音（series=「初音」）和定轴（fixed_axis 有值）比普通花色贵，客服容易把普通花色备注发到这些订单，要在价格速查页单独警示。
- **实现**（index.html，上半场 JS 由上一会话写完，本会话收尾）：
  - 数据层：`PP_SPECIALS` 配置组（key/label/color/match），`ppSpecialTags(p)` 返回命中标签；以后加第三类特殊品只需在数组加一条。
  - 第一层：四分类大卡下方，当前品牌有命中才显示 `pp-special-head` 粉色警示条 + 每类一张红卡（`--c` 玫红/橘红主题色），点红卡直达该类明细（跳过第二层）。
  - 第二层（special 汇总）：红卡列表 + 去重计数（同款同时命中初音+定轴只算 1 款），搜索滤空时显示「😕 当前筛选下没有高价特殊品」。
  - 第三层（明细）：顶部红色 `pp-special-banner`「禁止发送普通花色备注」，按价格档分组（与按直径进入的分组规则一致），返回按钮文案「← 返回高价特殊品」。
  - 卡片角标：`ppCardHtml` 里 `spBadge` 拼进 `.pp-img`，右下角玫红渐变角标「⚠️初音 · 定轴」（双命中显示双标签），`pointer-events:none` 不挡灯箱点击。
  - CSS：补齐 `.pp-group-title.special`/`.pp-special-badge`/`.pp-special-head`/`.pp-special-banner` 四条（上一会话漏写，角标此前根本没拼进 HTML）。
  - 修复：第二层特殊品标题计数原来误用 items.length（整品牌款数），改为 `ppSpecialTags` 去重后的特殊品款数。
- **测试**：smoke-pp.js 新增 specialZone 独立数据集块（s1-s5，含双命中 s5）15 布尔 + 7 数值断言：L1 红区出现/客服无勾选框、直达明细 banner/角标/按价分组、汇总层去重计数「共 3 款」、搜索排除→红区消失、只命中单规则→只剩一张红卡、管理员批量条/勾选框/快捷下拉。全部 PRICE_OK，语法 5 段 inline script 全过。
- **踩坑**：①搜「初音」验证单红卡翻车——双命中款 s5（初音+定轴）让两张红卡都在，改搜「初音粉」（只命中单规则的款名）才对；②截图脚本首层预览一度以为批量条泄漏到 L1，用「强制隐藏后字节对比」+elementFromPoint 探针证实是看了旧缓存图，DOM 状态一直正确，别被预览图骗了。
- **部署**：无需 SQL（纯前端读现有 series/fixed_axis 字段）、无需 bump iframe 版本；预览图 pp-page-preview.png（首层含红区）/ pp-special-preview.png（初音明细）已更新。

## 初音联名机制上图 + 单位修正（杨茹：机制图同步到卡片首页，初音日抛按盒）
- **关键 bug 修复**：查线上数据发现初音真实系列名是「初音-日抛」（4 款日抛，49.9元/副）和「初音未来」（9 款半年抛，未标价），原 match 全等 `=== '初音'` **一条都匹配不上** → 改为 `series.includes('初音')`。
- **机制上图**：`PP_SPECIALS` chuyin 加 `mech` 数组（49.9/1盒·赠明信片+佩戴棒、89.8/2盒·+随机徽章、149.7/4盒·徽章*2、199.4/6盒·徽章端盒，改内容直接改这个数组）；`ppMechHtml(sp, withTitle)` 红卡/明细页复用；首层与汇总层初音红卡直接印 4 档机制（spCard 构造器去重了原来两处重复代码），明细页 banner 下机制条常驻。
- **单位规则**：`ppTierLabel(p)` 卡片价格标签——初音日抛「/副」→「/盒」（只改显示不动库档位名），半年抛保持副；明细页分组标题按组内成分判断（组内全是初音日抛才转盒，混组不转）。注意管理员快捷下拉/批量下拉里仍是原始档位名（共享数据不改）。
- **测试**：smoke specialZone 种子改为线上真实系列名（初音-日抛/初音未来），新增 spMechCardOk（首层卡面机制 4 档+赠品）/spMechRows=4/spMechDetailOk（明细机制条）/spUnitBoxOk（49.9元/盒 且无 49.9元/副）/spUnitPairOk（59.9元/副 保持）断言，67 项全过 PRICE_OK；语法 5 段全过。预览图已更新（首层红卡印机制 + 明细页机制条/盒单位）。
- **部署**：commit 41eb773，main 已 push，无需 SQL、无需 bump。

## 机制拆两组：日抛按盒 / 半年抛按副（杨茹截图反馈：69/副是半年抛机制，与 49.9/盒的日抛机制要分开写）
- **改动**：`PP_SPECIALS` chuyin 的 `mech` 单数组重构为 `mechs` 多组结构，每组 `{short, title, rows}`；日抛组（49.9/1盒、89.8/2盒、149.7/4盒、199.4/6盒）+ 半年抛组（69/1副·小卡+护理液+伴侣盒、99.8/2副·+镭射、139.8/3副·镭射+徽章+护理液+6联盒、232.5/5副·+立牌+亚克力手持镜）。`ppMechHtml(sp, full)` 遍历 mechs，卡面用 short 标题（日抛 · 按「盒」/ 半年抛 · 按「副」），明细页用完整标题。
- **同步发现**：杨茹已把 9 款初音未来半年抛打上「69元/副」，明细页两个分组（49.9元/盒 日抛组 + 69元/副 半年抛组）与两组机制正好一一对应；单位规则不变（日抛显示盒、半年抛显示副）。
- **测试**：smoke 断言更新（两组标题/8 行机制/半年抛组价格与赠品/s5 种子改 69元/副），PRICE_OK 全过；语法全过。
- **部署**：commit 34eb78c，main 已 push，无需 SQL。

## 警示文案方向修正（杨茹：写反了）
- **问题**：「禁止发送普通花色备注」读起来像「不许给普通花色发备注」，实际规则是反的——**初音等特殊品禁止按普通款发备注**。
- **改动（3 处）**：①明细页横幅 →「⚠️ 此系列花色禁止按普通款发备注！发备注前务必先核对本页价格」；②红卡副标 →「共 N 款 · 禁止按普通款备注此系列」；③首层警示条 →「价格更高，禁止按普通款发备注，发前务必核对价格！」。smoke 断言同步更新，PRICE_OK。
- **部署**：commit d38039d，main 已 push。

## 特殊品明细分组标题加抛型大字标签（杨茹：不熟悉的人不知道初音/定轴哪些是日抛哪些是半年抛，顶部大字显示更直观）
- **改动**：特殊品（isSpecial）明细页每个分组标题前插入 `pp-gtype` 彩底白字大字标签——组内抛型去重 `tset` 后仅一种则按 `PP_TYPE_COLORS` 配色显示该抛型名（日抛蓝/月抛紫/季抛青/半年抛橙/年抛红），混组（如含多抛型）则灰色兜底「混合抛型」。CSS `.pp-gtype{font-size:15px;font-weight:800;padding:3px 14px;border-radius:10px;flex-shrink:0;box-shadow}`，随 `.pp-group-title` flex 排在组名前。仅特殊品详情页启用，普通价格分组不受影响。
- **测试**：smoke 新增 `spTypeChips`（=2 数量断言）+ `spTypeChipOk`（日抛与半年抛标签同时存在布尔断言），PRICE_OK 全过。
- **踩坑（测试脚本登记笔误）**：`spTypeChips` 存的是数量 2，却误同时写进「布尔 needed 列表」（要求 ===true），数字 2 永远不等于 true → 全链路误报 FAIL。**教训：计数类断言只能进 numeric 列表，布尔语义单独用 *_Ok 字段**。同类笔误还可能藏在其它历史断言里，改断言时注意分清。
- **部署**：commit 52b1616，main 已 push，无需 SQL、无需 bump。

## ⚠️ 本地 .git 对象库损坏记录与恢复方案（2026-09-04 发现，未完全修复）
- **现象**：commit 触发 geometric repack 报 `bad tree object 3cd012a9`；`git fsck --full` 大量 missing blob/tree/commit；`refs/remotes` 全空（`git status` 显示 `origin/main [gone]`）；pack 目录多个 `.idx` 无对应 `.pack`（孤儿索引）。疑似更早某次 repack/gc 中断留下。
- **应急处理（已做）**：`git config gc.auto 0` + `git config maintenance.auto false` 禁掉崩溃触发源。**增量 commit/push 完全正常**——push 只需发送 `远端HEAD..本地HEAD` 的增量对象，不依赖损坏的旧历史。后续 52b1616/cba40ef/6564284 均已正常推送。
- **恢复方案（进行中）**：后台全量克隆到 `C:/Users/Administrator/WorkBuddy/_recovery_kefu`（约 100M+，沙箱网慢需 40-60 分钟）→ 克隆完成后先 `git fetch` 补上克隆启动后新推的提交 → 校验克隆 HEAD 与项目本地 HEAD 一致 → 把项目 `.git` 改名备份 → 克隆的 `.git` 移入项目 → `git status`/`git log` 验证。**若换库未做，下次会话先完成这一步。**
- **教训**：①沙箱网慢，前台 clone 大仓库会撞 120s 超时被 SIGTERM，要用 run_in_background；②本地 git 报对象错误时先 `git ls-remote` 对比远端，确认远端健康后走「增量 push + 后台换库」，不要急着在损坏库上反复 repack 修复。

## 定轴红卡新增少女漫机制（杨茹发机制表截图：写到定轴分类首页，初音除外不能选）
- **改动**：`PP_SPECIALS` dingzhou 条目加 `mechs` 三组 + `note` 字段——①单副·到手同价（正常款/定轴款均 59.9，定轴原价69.9）；②满减50·2副（正常×2=69.8、正常+定轴=79.8、定轴×2=89.8，各行 gift 带原价/均价/省50）；③满减80·3副🔥（99.7/109.7/119.7/129.7，正常×3 均33 最划算）。`note`=「本机制仅限少女漫定轴款 + 普通款混搭参与 · 初音系列除外不能选」。
- **实现**：`ppMechHtml(sp, full)` 顶部渲染 `sp.note`（`.pp-mech-note` 橙色虚线警示框），卡面与明细页共用；行结构复用 {price=到手价, boxes=组合方案, gift=原价/均价/节省}，渲染器零改动。初音条目无 note 不受影响。
- **注意**：定轴机制是「可混搭普通款」，与初音的「禁止按普通款发备注」规则方向相反，两类警示文案不要混用。
- **测试**：smoke 新增 spDzMechOk/spDzNoteOk/spDzDetailOk（明细页机制完整标题+注释）/spDzMechRows=17（首层双卡机制行合计）断言，PRICE_OK 全过；语法 7 段 0 错误。
- **部署**：commit 4ac5939，main 已 push，无需 SQL。
- **返工（杨茹反馈：容易混淆）**：定轴警示框去掉「初音系列除外不能选」——初音板块自己已有警示，机制文案里再提前音反而混淆。定轴 note 最终为「本机制仅限少女漫定轴款 + 普通款混搭参与」。**文案原则：每个特殊品板块只说自己的规则，不跨板块引用别的系列**。smoke 断言同步改为「警示框文字不含初音」。commit f857fae。
- **二次返工（杨茹澄清：指的是花色列表重复）**：定轴红卡计数与明细列表要去掉初音系列花色（它们已在初音分类）。新增 `ppSpecialOwn(s,list)`——按 PP_SPECIALS 数组顺序归属，命中最前规则（初音优先于定轴），首层红卡计数/汇总层/明细列表三处口径统一；**普通分类里卡片的「初音·定轴」双角标保留**（提示双属性，不冲突）。以后新增第三类特殊品时注意数组顺序即归属优先级。smoke 新增 spDzDetailCards=1/spDzOwnOk 断言。commit a5552d7。
- **三次调整（杨茹：满减去掉只留数量+价格；机制字密密麻麻）**：①dingzhou mechs 标题改「1副/2副/3副·最划算」，行删掉原价/均价/省N，只留到手价+组合（组合名含定轴款×N）；②`.pp-mech-row` 排版优化——行距 padding 3.5px+虚线分隔（last-child 去线）、价格 b 18px+min-width:52px 对齐、组合名 13px、gift 12px；③`ppMechHtml` 空 gift 不渲染空 span。**机制文案原则：只写客服要念的数量+到手价，促销计算细节（原价/均价/满减）不上图**。smoke 断言含「全文无满减」。commit 048d653。
