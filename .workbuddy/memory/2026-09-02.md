# 2026-09-02 工作记录

## 质检工具 qc-v2.html — Request B 落地（杨茹）
- **质检分析配图**：`renderAnalysisSheet` 改 async，时间线（最近10条）每条渲染 场景/回复/知识点 缩略图，点击 `previewImage` 放大；`analysisThumbsHtml` 新函数；`renderAllStaffSummary` 同步改 async。
- **一键发送（本人+负责人）**：分析弹窗、讲解单弹窗 footer 各加「📤 发送(本人+负责人)」按钮（`analysisSendBtn`/`coachSendBtn`，仅管理员可见）。
  - `sendAnalysisReport` / `sendCoachSheet`：站内信通知该客服（本人，复用 `qcNotifyStaff`）+ 全部管理员/组长（`qcNotifyAdmins` 新函数查 profiles role in admin/leader），并生成深链。
  - 深链：`qc-v2.html?share=analysis|coach&staff=<姓名>` → `handleDeepLink()` 在 loadRecords 末尾（及 DEMO init）自动打开对应弹窗并选中客服。
- **清空 9/1-9/2**：生成 `clear_qc_0901_0902.sql`（先 SELECT 核对 → 删 storage.objects 物理文件 → 删 qc_shares(BIGINT[] 包含匹配) → 删 qc_records 级联 qc_images/qc_image_snapshots）。**待用户在 Supabase 云端 SQL Editor 手动执行**（本沙箱无法直连 DB）。
- 验证：inline 脚本 `new Function` 语法自检通过；Edge 无头冒烟（?demo）通过——分析时间线含 `timeline-imgs`、两处发送按钮可见、深链解析无错；唯一控制台 404 为 favicon，无影响。
- 部署：commit `bab1ef9`，index.html iframe `qc-v2.html?v=20260904a`（强制刷新缓存），main 分支直接生效。

## 待用户操作
- 在 Supabase SQL Editor 执行 `clear_qc_0901_0902.sql`（先跑 0 核对，确认只有 9/1-9/2 再继续），然后重新登记。
- 客户端需 `Ctrl+Shift+R` 强刷以跳过 GitHub Pages/CDN 缓存。
