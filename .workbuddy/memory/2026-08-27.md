## 2026-08-27

### 培训资料 Tab 样式优化
- **改动**：`index.html` 大类/小类 Tab 标签放大、间距加大
  - 大类：padding 11px 28px，font-size 16px，gap 12px
  - 小类：padding 9px 22px，font-size 15px，gap 10px
  - 新增大类/新增小类按钮同步放大，编辑输入框和按钮尺寸适配
- **验证**：inline script 语法校验通过；无头 Edge 冒烟测试通过；截图确认视觉饱满
- **部署**：commit `1630d12`，已 push 到 `main`，GitHub Pages 回源生效

### 培训资料内容区卡片化
- **改动**：`index.html` 把资料链接从列表改为响应式卡片网格
  - 卡片尺寸 260px+，hover 上浮 + 阴影
  - 每张卡片含分类图标、标题、品牌 badge、标签
  - 图标按小类/标题智能匹配（抛型/材质/直径/含水/ERP/店铺/品牌等）
  - 整卡可点击打开飞书链接，管理按钮悬浮显示
- **验证**：inline script 语法校验通过；无头 Edge 冒烟测试通过；截图确认卡片效果
- **部署**：commit `31cf163`，已 push 到 `main`，GitHub Pages 回源生效

### 培训资料页面内知识卡（美瞳抛型样板）
- **改动**：
  - 新增 `training_knowledge` 表（subcat/section_type/title/icon/content/sort_order）
  - 前端加载并按小类渲染知识卡，支持 overview/cards/steps 三种版式
  - 选中具体小类时优先展示知识卡，下方仍显示该小类资料链接
  - 插入美瞳抛型示例种子（抛型对比总览/佩戴护理要点/长周期抛护理步骤）
- **验证**：inline script 语法校验通过；无头 Edge 冒烟测试通过；截图确认知识卡效果
- **部署**：commit `2915e7f`，已 push 到 `main`，GitHub Pages 回源生效
- **待执行**：用户需到 Supabase SQL Editor 执行 `add_training_knowledge.sql`，刷新后进入「基础知识 → 美瞳抛型」查看

### 培训资料卡片图标去花朵
- **改动**：`index.html` 资料卡片图标替换
  - 弥生：🌸 → 📘
  - 极氧：🫧 → 📗
  - 其他小类图标保持不变
- **验证**：inline script 语法校验通过；无头 Edge 冒烟测试通过；截图确认品牌资料卡片已换为中性文档图标
- **部署**：commit `d1a2011`，已 push 到 `main`，GitHub Pages 回源生效

### 培训资料卡片点击详情弹窗
- **改动**：
  - 新增 `add_training_desc_column.sql`：给 `training_materials` 加 `description` 字段存放详细注解
  - 编辑弹窗增加『注解/详细描述』多行输入框
  - 卡片点击由直接跳转改为弹出居中详情弹窗
  - 弹窗展示：标题、品牌 badge、标签、详细注解、打开原文按钮
  - 无注解时隐藏描述区块，不影响现有交互
- **验证**：inline script 语法校验通过；无头 Edge 冒烟测试通过；截图确认弹窗与注解展示正常
- **部署**：commit `7e3e8c0`，已 push 到 `main`，GitHub Pages 回源生效
- **待执行**：用户需到 Supabase SQL Editor 执行 `add_training_desc_column.sql`，之后可在编辑弹窗填写注解
