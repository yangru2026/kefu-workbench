# 2026-09-09

## 新增：赠品编码查询页（gift.html）
- 需求来源：茹姐给的弥生赠品表（产品图/简称/成本/商家编码），要做成工作台页面，客服查编码+成本+图片，且后续她自己在页面维护，卡片视图。
- 交付：
  - `gift.html`（独立单文件，iframe 接入）：卡片视图（图片+简称+成本+商家编码可点击复制）、实时搜索（简称/商家编码）、图片灯箱；admin 可新增/编辑/删除 + 上传图片，其他角色只读。
  - `create_gift_codes.sql`：建 `public.gift_codes`（name/merchant_code/cost/image_path/sort_order）+ RLS（登录可读、role='admin' 可写）+ 公开 bucket `gift-images`（公开读、admin 写）+ 预置弥生 10 条赠品数据。
  - `index.html`：导航新增 🎁「赠品编码」+ `page-gift` iframe 容器（懒加载，同质检工具模式）+ switchPage 分支。
  - `outputs/smoke-gift.js`：gift.html 冒烟脚本（本地起 8127 端口服务 + Edge headless）。
- 权限口径：赠品维护**仅 admin**（未开放 leader，需开放时改 gift.html 里 `isAdmin = (role === 'admin')`）。
- 踩坑：gift.html 里 `const supabase = window.supabase.createClient(...)` 与 UMD 全局变量 `supabase` 重名 → `Identifier 'supabase' has already been declared`，页面脚本整体不执行。改用 `const sb = ...` 后正常。**新写 supabase 独立页时一律别用 `supabase` 做变量名**。
- 验证：node 内联脚本语法检查全过；无头冒烟 SMOKE_PASS（无 pageerror，未登录正确显示遮罩 + 🔒 未登录角标）。
- 部署：commit `e398679` 已 push origin/main，GitHub API 确认 gift.html 已上线。
- 待茹姐执行：Supabase SQL Editor 跑一次 `create_gift_codes.sql`，之后才能在页面里看到 10 条赠品并上传图片。

## 修复：赠品页 iframe 塌陷（当天下午，commit 0f1f0c4）
- 现象：茹姐截图反馈「卡住了」，页面只显示一排卡片顶条（约 67px 高的图区），下方全空。
- 排查：无头模拟管理员+10条数据正常渲染 → 对比截图像素测量（卡片列宽 180px 与代码吻合、图区仅 67px、下方无 card-body）→ 定位为 **iframe 塌陷成默认 150px 高**，不是数据/渲染问题。
- 根因：index.html 的 `.page.active{display:block}`，容器 flex 规则无效；qc 页靠专属规则 `#page-qc.active{display:flex;height:100vh}` 撑满，新建 iframe 页漏了这条。
- 修复：`#page-qc.active, #page-gift.active { display:flex; height:100vh; overflow:hidden; }`，gift.html 缓存参数升 `v=20260909b`。无头验证 iframe 高度 150→868 全屏。
- **套路沉淀：以后每加一个 iframe 型子页面，必须给 `#page-xxx.active` 加 `display:flex;height:100vh;overflow:hidden`，并把缓存参数升版。**
- 新增冒烟脚本：`outputs/smoke-gift-admin.js`（拦截 SDK getSession 模拟管理员+mock 数据）、`outputs/smoke-gift-embed.js`（嵌入后量 iframe 高度）。
- 截图诊断技巧：用 PIL 按颜色像素扫描测量元素位置尺寸（`.card-img` #f8fafc、边框 #e2e8f0），可直接判断「页面渲染成什么样」，比肉眼猜准。

## 花色素材页改版：素材库信息流样式（当天下午，commit c26216e）
- 需求：茹姐发小鹅通素材库截图，要把花色素材页改成「有图 有描述 可复制」的信息流。
- 改动（全在 index.html，数据/管理逻辑未动）：
  - 顶部新增 `.pf-chips` 横滑分类条：🆕 最新(30天内上新)+全部+色系分类（复用 getMergedCats('color',...)）；点击即筛（setPatternChip/setPatternLatest）。
  - 原 9 维筛选卡折叠进「⚙️ 更多筛选」（togglePatternAdvFilters），带生效筛选数角标（updatePatternAdvBadge）；搜索框移到折叠卡外面常显。
  - 卡片模板改为 `.pfeed-card`：品牌头像(前2字)+花色名+系列tag+日期 | 推荐话术（>70字 全文/收起 togglePatternDesc）| 多图宫格（eye+lens 合并，最多6格+「+N」遮罩，点击进原灯箱）| 参数 tags | 操作行：⬇️下载图片(downloadPatternImages，fetch blob 逐张下载，跨域失败退化为新窗口打开) + 📋复制话术(copyPatternScript 加 dataset.origLabel 恢复原文案) + 管理按钮（.pfeed-actions .pcard-mgmt{position:static}）。
  - 新 CSS 块插在 page-patterns 后；`#page-patterns .pattern-grid` 覆盖为 flex 纵向列表 max-width 860px。
- 冒烟：outputs/smoke-pattern-feed.js（mock pattern_assets 2行+pattern_categories 2色系），断言 chips/卡片数/图片数/按钮/全文/更多筛选/chip 筛选/最新筛选 全过，无 pageerror。
- 注意：renderPatternChips/updatePatternAdvBadge 挂在 renderPatternFilters 末尾，品牌/抛型切换后 chips 自动刷新；switchPatternBrand 会重置 patternLatestFilter。

## 气泡化微调（当天，紧随信息流改版）
- 茹姐反馈顶部标签太死板（品牌tab整行大横框 + chips 长方条）→ 气泡化：
  - `#page-patterns .brand-tab` 去 flex:1，改独立半透明毛玻璃气泡（不规则圆角 18/24/20/26 + 内高光 + 模糊背景）；`.brand-tabs` 容器去白底阴影改透明横滑。
  - `.pf-chip` 改 6 色循环淡染半透明气泡（nth-child(6n+x)），不规则圆角，::before 有机小圆点，hover 微抬升微旋转，active 品牌绿渐变（!important 压过色染）。
  - `.pf-adv-toggle`、`.pattern-filters-card`（#page-patterns 作用域）同步毛玻璃半透明。
- 注意：`.brand-tabs` 全局样式在 index.html ~line 469-477（flex:1 整行条），其他页面若也用 brand-tabs 需单独评估。

## 信息流改两列（当天）
- 茹姐反馈宽屏下单列右侧空太多 → `#page-patterns .pattern-grid` 改 `grid-template-columns:repeat(2,1fr); align-items:start`，<=920px 单列。冒烟脚本 viewport 改 1920 复跑通过。
- 赠品页追加：成本标签加「成本价」前缀（a7f1ca5）；编辑弹窗支持 Ctrl+V 粘贴图片/截图（document 捕获级 paste 监听 + setPendingImage 抽公共函数，e753c2d，v=20260909d）。
- 飞书花色同步答疑：insert_only 按 name+brand 匹配，已有文字字段只补空不覆盖，图片数组以飞书为准覆盖，下架标记不可补录——详见 supabase/functions/feishu-sync/index.ts（fillEmptyPatternFields / syncExistingPatternImages）。
- 修复赠品图片不显示（521b821）：imgUrl 拼 public URL 时漏了桶名 gift-images，上传成功但展示 404。**坑：Supabase Storage 公开 URL 格式 = /storage/v1/object/public/{bucket}/{path}，手拼 URL 一定带桶名**（或改用 .getPublicUrl() 让 SDK 拼）。

## 性能优化：工作台加载超慢（用户截图反馈「加载超慢的」）
- 根因：①首页 ~1MB，其中 ~212KB 是**整段内联 supabase SDK**，无法单独缓存；②每次打开都向 supabase.co（Cloudflare 新加坡节点）重新拉全部表数据，杭州往返慢；③无顶层启动动画，下载解析期间白屏像卡死。
- 方案（茹姐选「核心提速 1+2+3」）：
  1. **删内联 SDK**：移除 `<script id="supabase-inline-lib">`，改由已有 `vendor/supabase.min.js`（本地优先 + 多 CDN 兜底 loader）加载 → 首页 1MB→812KB，SDK 单独可缓存（冒烟实测 vendor 加载 250ms 就绪）。
  2. **本地缓存静态数据**：新增 `kefuCacheGet/kefuCacheSet`（key=`kefu_cache_<table>`，版本号 `KEFU_CACHE_VER='v1'`）。5 个 load 函数（training_materials/categories/knowledge/scripts、pattern_assets）改「先秒显缓存→后台静默刷新→回写缓存」；gift.html 的 `loadGifts` 同样改造（key=`kefu_cache_gift_codes`）。首页打开先渲染缓存，体验秒开。
  3. **启动加载动画**：新增 `#boot-splash`（小布转圈 + 「正在加载工作台…首次加载稍慢，已为你缓存常用数据」），`initSupabase` 完成后 `hideBootSplash()`，外加 8s 兜底 + load 事件双保险，永不白屏卡死。
- 验证：node 内联脚本语法检查全过；puppeteer-core + Edge 无头（拦截 supabase 返回 mock，超时保护 evaluate）：首页 supabase 就绪、splash 显隐正常、无 pageerror、`kefu_cache_*` 5 个键全部写入；gift 页 `kefu_cache_gift_codes` 写入。switchPage 切页无冻结无报错。
- 提交：commit 待 push（本会话结束前）。注意：移除内联 SDK 后 `window.supabase` 初始化后会被覆盖为客户端实例（含 `.from`、无 `.createClient`），测试断言要用 `typeof window.supabase.from==='function'` 而非 `.createClient`。
- 预留未做（茹姐按需）：④花色素材图 WebP/缩略图 + `?width=` 变换（Supabase Storage transform）进一步压图。
