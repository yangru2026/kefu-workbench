# 2026-08-07 工作日志

## 尤赫客服工作台 - 质检工具+日报截图修复
- **质检工具页面遮挡修复**: `#page-qc.active` 添加 `height:100vh; overflow:hidden`，确保 iframe 在 flex 布局下正确撑满视口高度，不再出现内容溢出/遮挡问题
- **日报截图速度优化**: 
  - html2canvas `scale` 从 2 降到 1.5（减少约44%渲染像素）
  - 添加 loading 遮罩动画（旋转spinner + "正在生成截图..."文字）替代纯 toast
  - 用双层 `requestAnimationFrame` 确保 loading UI 先渲染再执行耗时操作
  - 关闭 `useCORS`（日报卡片无跨域图片），添加 `imageTimeout:5000` 防卡死
- 已提交并推送到 GitHub Pages
- **后续再次优化（用户反馈仍慢）**：
  - `copyDailyResult()` 改为只截取 `.daily-screenshot-card` 内部卡片，不再截取外层 wrapper
  - html2canvas `scale` 从 1.5 降到 1，渲染像素再减少约 55%
  - 通过 `ignoreElements` 排除截图区域内的 `<button>`，减少 DOM 渲染量
  - 添加 10 秒超时保护：超时时自动关闭 loading 并提示重试/手动截图
  - 成功/失败/超时后统一清理 loader 与定时器
  - 提取 `downloadScreenshot()` 函数复用下载 fallback
  - 已再次提交并推送到 GitHub Pages（commit 8914cd0）
- **第三次修复（html2canvas 彻底卡死）**：
  - 彻底弃用 html2canvas，改为用原生 Canvas 2D API 自己绘制日报卡片
  - 新增 `renderDailyReportCanvas()`：手动绘制头部、数据标签、表格、汇总、备注，绕过 DOM 解析
  - `copyDailyResult()` 改为直接调用 Canvas 绘制并生成 PNG blob，复制到剪贴板或下载
  - 保留 loading 动画，但不再依赖第三方库，几乎瞬间完成
  - 已提交并推送（commit 7a935af）
- **Canvas 截图头部布局微调**：
  - 用户反馈“售前”徽章与“总接待量/数字”上下挤在一起
  - 将姓名/分组与总接待量改为同一行左右分布，拉开与顶部“售前”徽章的间距
  - 头部高度从 100 增加到 124，分隔线位置同步下移
  - 已提交并推送（commit 7c0cd80）
- **性能优化 + 花色下架标记**（commit 5fadcd5）：
  - 首屏：移除 head 中 tesseract.js、html2canvas（已不再使用），Tesseract 改为 performOCR 时按需动态加载
  - onSupabaseReady 改为按当前页按需加载，不再预加载培训/花色等全部模块
  - switchPage：培训页首次进入才加载 training 数据；花色页去掉 loadPatternsFromDB 后重复 render；花色搜索框加 250ms 防抖
  - qc-v2.html：优先复用父窗口 supabase client，避免 iframe 重复下载 supabase-js
  - 花色下架：pattern_assets 新增 is_discontinued 字段（SQL 文件 add_discontinued_column.sql，需用户在 Supabase 执行）；卡片增加 📦/↩️ 标记按钮和「已下架」角标；品牌栏新增「📦 下架花色」分类入口，点击只看下架花色
- **飞书云文档关联**：待用户澄清文档类型与是否有开放平台应用，方案倾向于 GitHub Action 定时同步飞书多维表格 → Supabase
